# sentry-fix run — 2026-08-28

## Issues triaged (2 new ids, 1 reactivation)

- **7696205115** (-67, `fetchVideoPostCounts` PGRST303) — post-deploy regroup
  of ledgered -64. **wontfix**: platform fault; Brad's 08-27 decision stands
  (leave to Supabase, resurface if firing weeks later). But it has ESCALATED:
  13 errors in ~16 bot ticks on 08-28 (was ~32% on 08-26/27). PR #30's
  fail-closed caps mean the bot posts nothing on erroring ticks — the bot is
  now mostly paused. That is the designed safe degradation, but Brad should
  know the availability cost grew.
- **7697361547** (-68, `tryAcquireCronLock` PGRST303) — regroup of -66; 2
  events 08-28: `reminder_v2_cron` 08:11Z + `trending_cron` 15:11Z.
  **claimed**: the day-skip defect proposed Tier 0 on 08-27 was exercised on
  user-facing jobs, and it lives in in-grant code (`app/lib/**`, no security
  surface) — the 08-27 Tier 0 call was PR-scoping, not a grant limit.
- **7448979905** (-22, listConversations) — 2 events after 13-day dormancy;
  latest event is status 429 text/html (Shopify edge rate-limit), not the 503
  class. Noise; ledger note updated in place (retry correctly skips 4xx).

## Root cause (claimed issue)

`tryAcquireCronLock` returned the same `false` for "lost the day-lock race"
and "lock query errored"; every cron caller maps `false` to `lastRunDate =
today`. One transient PGRST303 therefore made the erroring machine mark the
day done and silently skip its daily job (reminder emails, trending, ops
summaries) unless the other machine happened to win the lock.

## Fix

PR: https://github.com/DrBradStanfield/roadmap/pull/35 (Tier 3 pipeline,
branch `claude/sentry-fix-cron-lock-day-skip`). US-28 added to
docs/user-stories.md (+html regenerated). Transient lock errors now throw
(US-27's fail-distinguishably convention); reminder-v2's acquire moved inside
its tick try/catch (the one caller shaped differently — a throw would have
been an unhandled rejection); the other three callers inherit retry via their
existing catch. Helper's own Sentry captures deleted (callers capture; old
shape double-reported). Failing-first: the two AC1 rejection tests fail on
pre-fix code. Full suite: `1434 passed | 1 skipped (1435)`; tsc clean on
touched files.

## Escape analysis

`tryAcquireCronLock` had zero unit tests — only ever `vi.mock`ed in caller
suites, so its error semantics were pinned nowhere. Second instance of the
US-27 class; LEARNINGS entry extended in place (now includes an audit
shortcut: grep error branches returning false/0/{} in cron/cap call paths).
Bonus latent hazard found by the new test: reminder-v2-cron self-starts on
import and lacked the `NODE_ENV=test` guard — fixed in the PR.

## Follow-up proposals (Tier 0, from /simplify's 4 reviewers)

1. `NODE_ENV=test` guard drift: chat-summary + youtube-bot-summary crons
   self-start on import guarding only 'development' — same latent test hazard
   just fixed for reminder-v2. Two 1-line changes.
2. Shared `runDailyCron` tick helper (interval + hour gate + lastRunDate +
   lock-in-try + tagged catch) — would enforce US-28 AC2 structurally across
   all four crons instead of by convention; Lane-B-shaped, deferred.
3. Shared Supabase chain-mock test factory — cron-lock.test.ts is the third
   bespoke copy (after reminder-v2-typed, youtube-bot suites).

## Ledger / metrics

Ledger: -22 and -66 updated in place; -67 wontfix + -68 claimed added; both
claimed rows carry PR #35. Metrics: `2026-08-28,2,2,0,1,false`.

## Data gaps

- `SUPABASE_PRODUCT_HEALTH_KEY` in this env is REJECTED by PostgREST
  ("Invalid API key") — could not read `cron_lock` to confirm whether the
  other machine ran today's reminder/trending jobs (anon key works but RLS
  hides the table). Worked on 08-27 (25-probe test); Brad: check/rotate the
  loop's key. Whether 08-28 reminder emails went out is therefore
  unverified from here (Fly logs would show it).
- status.supabase.com still blocked by the egress proxy (as 08-27).

## Retro

- No-op fast path ran first and correctly did not fire (2 unledgered ids).
- Orchestrator on top tier (no fallback). Spend: 1 worker suite run, 4
  /simplify reviewers, 1 adversarial reviewer. The /simplify pass earned its
  cost: caught my inaccurate US-28 test-honesty claim (guard-drift fact) and
  cut the four duplicated comments before review.
- Waste: `npm run lint` is broken repo-wide (no ESLint config anywhere —
  predates this run); worker burned a step discovering it. Flagging, not
  fixing (config file location is Brad's call).
- Test-writing detour: the reminder caller test double-started the cron
  (module self-start + missing test guard) — cost two debug cycles but
  surfaced the guard fix; probe-first debugging (timer count) beat theorizing.
- Size (`wc -l -c`): see adversarial-review section below (machine output).

## Adversarial review

See next commit amendment: reviewer runs after this report is committed
(truncation-proofing); findings fixed-or-rebutted here by name.
