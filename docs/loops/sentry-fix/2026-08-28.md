# sentry-fix run — 2026-08-28

## Issues triaged (2 new ids, 1 reactivation)

- **7696205115** (-67, `fetchVideoPostCounts` PGRST303) — post-deploy regroup
  of ledgered -64. **wontfix**: platform fault; Brad's 08-27 decision stands
  (leave to Supabase, resurface if firing weeks later). It has escalated
  moderately: 13 errors over ~32 half-hourly bot ticks on 08-28 ≈ 40% (was
  ~32% on 08-26/27). PR #30's fail-closed caps mean the bot posts nothing on
  those erroring ticks — designed safe degradation, availability cost growing
  slowly.
- **7697361547** (-68, `tryAcquireCronLock` PGRST303) — regroup of -66; 2
  events 08-28: `reminder_v2_cron` 08:11Z + `trending_cron` 15:11Z.
  **claimed**: the day-skip defect proposed Tier 0 on 08-27 was exercised on
  user-facing jobs, and it lives in in-grant code (`app/lib/**`, no security
  surface) — the 08-27 Tier 0 call was PR-scoping, not a grant limit.
- **7448979905** (-22, listConversations) — 2 events after 13-day dormancy:
  status 429 text/html (Shopify edge rate-limit). Noise; ledger note updated
  in place (retry correctly skips 4xx).

## Root cause (claimed issue)

`tryAcquireCronLock` returned the same `false` for "lost the day-lock race"
and "lock query errored"; every caller maps `false` to `lastRunDate = today`.
One transient PGRST303 thus made that machine mark the day done and silently
skip its daily job unless the other machine happened to win the lock.

## Fix

PR: https://github.com/DrBradStanfield/roadmap/pull/35 (Tier 3 pipeline,
branch `claude/sentry-fix-cron-lock-day-skip`). US-28 added to
docs/user-stories.md (+html regenerated). Transient lock errors now throw
(US-27's fail-distinguishably convention); reminder-v2's acquire moved inside
its tick try/catch (the one caller shaped differently — a throw would have
been an unhandled rejection); the other three callers inherit retry via their
existing catch. Helper's own Sentry captures deleted (callers capture; old
shape double-reported). Failing-first: the two AC1 rejection tests fail on
pre-fix code. Full suite: `1434 passed | 1 skipped (1435)`; tsc clean on
touched files.

## Escape analysis

`tryAcquireCronLock` had zero unit tests — only ever `vi.mock`ed in caller
suites, so its error semantics were pinned nowhere. Second instance of the
US-27 class; LEARNINGS entry extended in place (now includes an audit
shortcut: grep error branches returning false/0/{} in cron/cap call paths).
Bonus latent hazard found by the new test: reminder-v2-cron self-starts on
import and lacked the `NODE_ENV=test` guard — fixed in the PR.

## Follow-up proposals (Tier 0, from /simplify's 4 reviewers)

1. `NODE_ENV=test` guard drift: chat-summary + youtube-bot-summary crons
   self-start on import guarding only 'development' (two 1-line changes) —
   the hazard just fixed for reminder-v2; pipeline reviewer seconded it.
2. Shared `runDailyCron` tick helper — would enforce US-28 AC2 structurally
   across all four crons instead of by convention; Lane-B-shaped, deferred.
3. Shared Supabase chain-mock test factory — cron-lock.test.ts is the third
   bespoke copy (after reminder-v2-typed, youtube-bot suites).

## Ledger / metrics

Ledger: -22/-66 updated in place; -67 wontfix + -68 claimed added (PR #35
on both claimed rows). Metrics: `2026-08-28,2,2,0,1,false`.

## Data gaps

- `SUPABASE_PRODUCT_HEALTH_KEY` in this env is REJECTED by PostgREST
  ("Invalid API key"; worked 08-27) — could not read `cron_lock` to confirm
  whether the other machine ran today's reminder/trending jobs, so whether
  08-28 reminder emails went out is unverified from here (Fly logs would
  show it). Brad: check/rotate the loop's key.
- status.supabase.com still blocked by the egress proxy (as 08-27).

## Retro

- No-op fast path ran first and correctly did not fire (2 unledgered ids).
- Orchestrator on top tier (no fallback). Spend: 1 worker suite run, 4
  /simplify reviewers, 1 adversarial reviewer — both review layers earned
  their cost (caught the guard-drift misclaim and the tick-rate error).
- Waste: `npm run lint` is broken repo-wide (no ESLint config exists —
  predates this run); flagging, not fixing (config shape is Brad's call).
- The reminder caller test double-started the cron (module self-start +
  missing test guard) — two debug cycles, but it surfaced the guard fix.
- Size (`wc -l -c`, machine output): `95 5024 2026-08-28.md` (pre-amendment) ·
  `96 6664 LEARNINGS.md` · `106 5589 LOOP.md`. All within caps.

## Adversarial review (1 round, fixed-or-rebutted by name)

4 findings. FIXED: (1) tick-rate arithmetic — "13/16 ticks, bot mostly
paused" used an hourly denominator on a half-hourly bot; true rate ~40%
(13/~32), corrected here + ledger + LEARNINGS + PR body. (2) size counts
restored to this retro (the committed v1 pointed at a not-yet-written
section). (3) ledger "all 5 cron callers" → "4 call sites / 5 lock names".
NOTED, not code-changed: (4) deleting the helper's captures drops the
per-lockName feature tags (`chat_summary`→`chat-summary` etc.; both trending
lock names now tag `trending_cron`) and machineId extras — future triage must
key on the error MESSAGE (which carries the lock name), not tags; recorded
here as the operative caveat. Reviewer confirmed everything else: Sentry
counts re-derived live, grant compliance, revert-check (tests fail on main's
code), full suite 1434/1, CSVs parse, PR #35 head = approved sha, NODE_ENV
guard safe in prod (Dockerfile sets production), data-gap claim reproduced.
