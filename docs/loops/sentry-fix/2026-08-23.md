# sentry-fix run — 2026-08-23

## Issue triaged

**7686931961 / JAVASCRIPT-REMIX-62** — `Error: Invalid URL. Refusing to
redirect`, 6 events, all 2026-08-22 (20:12 and 21:51 UTC). Only un-ledgered
unresolved issue of 13; no regressions among ledgered issues (both `fixed`
entries, #11 and #19, remain resolved in Sentry — absent from the unresolved
list).

## Root cause (from event data)

All six events are one actor (same UA: Chrome 151 / macOS, region ams)
manually probing the Shopify OAuth exit-iframe endpoint on
`health-tool-app.fly.dev`:

- 20:12 — `?exitIframe=javascript:alert(document.domain)` ×2
- 21:51 — `?exitIframe=data:text/html,<h1>a</h1>…` built up keystroke by
  keystroke over 11 s (4 variants)

Classic open-redirect / XSS payloads. Each was rejected server-side by
`@shopify/shopify-app-react-router`'s `sanitizeRedirectUrl`
(validate-redirect-url.mjs → renderAppBridge → respondToExitIframeRequest),
which throws before any redirect or render — Sentry marks the event
`handled: yes`. The error IS the defense firing. The YouTube-bot breadcrumbs
in the event are process-global noise from the same server, unrelated.

**Verdict: wontfix.** No vulnerability, no user impact, no code change
warranted. `app/routes/auth.$.tsx` is auth code — excluded from this loop's
Tier 3 grant regardless — and suppressing the capture would delete a useful
probe-activity signal. Not decision-worthy: routine internet background
probing, control held; no issue opened.

## Escape analysis

Nothing escaped: no defect exists. Class learning recorded in LEARNINGS
(`[noise][server]`): probes on the OAuth surface arrive as handled
"Refusing to redirect" errors and are ledgered `wontfix` on sight.

## Ledger / metrics

- ledger: 7686931961 → `wontfix` (probe traffic, defense working).
- metrics: `2026-08-23,1,1,0,0,false` ("ours" = on our server, per the
  2026-08-12 convention that ours-but-noise counts as ours).

## Data gaps

- The agent proxy 403-blocks direct requests to `health-tool-app.fly.dev`,
  so no zero-credential live re-probe to confirm the non-reflective error
  response; the Sentry stack (throw inside sanitizeRedirectUrl, before
  renderAppBridge emits HTML) is the evidence the payload is never reflected.
- Sentry retains only the latest event's full payload at current tier; the
  six-event query/UA table came from the events-list endpoint, which did
  return per-event request data (`?full=true`) — the 08-12 "event-history
  pulls return 1 row" learning overstated the limit; it applies to older
  issues whose events aged out, not to a fresh issue.

## Retro

- Charter 106 lines / 5017 B; LEARNINGS.md 79 lines / 5501 B (`wc -l -c`
  machine output; both inside caps; fleet-doctor.js not on this disk).
- Fast path cost: 2 API calls to isolate the single candidate; 3 more to
  triage it. No workers spawned — one issue, all judgment, nothing
  mechanical worth delegating; the orchestrator's own pulls were cheaper
  than briefing a worker.
- Orchestrated on Fable (top tier) as required.
- Adversarial review: pending (run truncated if this line survives).
