# sentry-fix run — 2026-09-10

## Issues triaged

Two un-ledgered issues among 19 unresolved (one project in the org); none
carries `substatus=regressed` and the `is:regressed` query returns 0. Every
ledgered issue's `lastSeen` is ≤ 09-08 09:53Z, before the 09-08 run's probe.

1. **7723659158 / JAVASCRIPT-REMIX-6E** — `Error: Chat: Action failed`, 2
   events, 08:51:51.649Z and .965Z (316 ms apart). **Ours, real, claimed.**
2. **7724447239 / JAVASCRIPT-REMIX-6F** — `TypeError: undefined is not an
   object (evaluating 'window.webkit.messageHandlers')`, 1 event, 15:43Z.
   **Not ours.** An `unload` listener wrapped by `sentryWrapped`, no frame in
   our bundles; `messageHandlers` greps to nothing in source or the five
   built assets (release `2abf0ca`; HEAD `138c3c8` is docs-only above it).
   Google Search App 69 in-app webview, iPad iOS 26.6, on a blog article —
   `window.webkit.messageHandlers` is the WKWebView native bridge the GSA
   overlay's injected script calls on page exit. Same class as 7690759796
   and 7708790398; ledger-on-sight per the `[noise][widget]` learning.

## Root cause (from event data)

Server event, `feature=chat`, `handled=yes`, machine `e2861050f14d58` (Fly
`iad`; `syd` in the headers is the edge). Request: `POST
https://health-tool-app.fly.dev/api/chat` — the Fly host directly —
`User-Agent: curl/8.6.0`, `query: []` (no `signature`, `shop`, `timestamp`),
`Content-Length: 50`, JSON. Stack: `action` (api.chat.ts:617) →
`reportChatError` (:39). Client unknown: geo `US`, two requests 316 ms
apart. (A draft tied the pair to Brad's commit `7bf840e` "at 08:51"; that
was NZ time, twelve hours earlier — adversarial review, finding 1.)

Without a signature, `getAuthOrGuest` → `getAuthenticatedUser` →
`authenticate.public.appProxy`, which rejects by THROWING a 400 `Response`
(`@shopify/shopify-app-react-router@1.2.0`, `appProxy/authenticate.js:17`,
read from the package tarball and byte-identical to node_modules). The
route's outer catch-all treated it as our failure: Sentry event + 500.
react-router 7.18.3 returns a thrown Response as-is and never calls
`handleError` (`handleQueryRouteError`), so the framework's path is silent.
The four sibling `appProxy` callers (`api.events`, `api.ab`, `api.feedback`,
`api.measurements`) call it outside any `try`; chat alone had the defect.
A title query finds no earlier issue with this message, and `is:resolved
Chat` returns only the 09-07 probe issue — unsigned POSTs to the chat route
are not routine scanner traffic yet.

**PR:** [#99](https://github.com/DrBradStanfield/roadmap/pull/99) —
`sentry-fix: JAVASCRIPT-REMIX-6E unsigned /api/chat requests keep the proxy
check's 400 (US-15 AC9)`. Both catch-alls re-throw a thrown Response; the
two inner try/catch blocks around `getAuthOrGuest` are deleted (their one
branch is the outer catch's second rung). Failing-first tests in
`api.chat.test.ts` (POST and GET). Prod diff `8 added / 18 deleted` (4 of
the 8 are comment lines). Tier 3 review: APPROVE pinned to `7c63584`, two
non-blocking wording notes → docs-only `eb2cedc` (AC9 scoped to requests
that reach the proxy check; US-15 `Tests:` line names the suite) →
re-review APPROVE pinned to `eb2cedc`. Auto-ship's window runs from there.

## Escape analysis

Every chat suite mocks `appProxy` to succeed; no test ever exercised the
library's rejection SHAPE, and the only error-path test (AC4) pinned a
client error the route returns itself. A thrown Response is a third kind of
thing a catch-all sees (not a return, not our Error) and nothing named it.
Compounding half: US-15 AC9 + the `it.each` + a `[class][server]` LEARNINGS
entry with the one-grep audit (`appProxy` inside a `try`).

## Ledger / metrics

- ledger: 7723659158 → `claimed` (#99); 7724447239 → `not-ours`.
- metrics: `2026-09-10,2,1,1,1,false`.
- LEARNINGS: new `[class][server]` entry; `[noise][widget]` extended in place.

## Data gaps

- **No 2026-09-09 metrics row.** No sentry-fix commit on 09-09 and the
  trigger API exposes only `last_run` (09-10), so the gap is one day and
  undiagnosable from here.
- Container runs as root, so the two US-31 AC8 `chmod` tests fail on any
  source (identically on unchanged main); CI's `test` job is green on both
  PR commits.
- Fly logs unreachable: the 500 body the curl received is inferred from
  the route, not observed.
- Deploy, live verification and the Sentry resolve happen after auto-ship
  merges; this session is subscribed to #99 with an hourly check-in.

## Retro

- Cost: 2 probe calls, 2 latest-event pulls, 1 events-list pull, 2 title
  queries, one npm tarball, the full suite twice (~45 s each). Workers: two
  Sonnet suite runs, four /simplify agents, one adversarial reviewer;
  triage, root cause and the fix stayed here. Orchestrated on Fable.
- Wrong first, caught by /simplify: the fix as first written added a third
  rung above two inner try/catch blocks the outer catch already made
  derivable. Two of four agents found it; applied.
- Slow: the first test draft awaited a return value where the route now
  THROWS — one wasted suite run. The unit test sees the throw; only the
  framework sees a 400.
- Adversarial review (11 findings, all folded): the Brad-attribution was a
  UTC/NZ artefact (dropped everywhere: report, ledger, PR body); the report
  was 112 lines against the charter's 100; "5 of 8 comment lines" was 4;
  machine `iad` vs edge `syd`; the quoted pre-fix failure text; the
  LEARNINGS entry ran 13 lines against the 1–3 sentence rule (compressed to
  8); the PR moved to `eb2cedc` after the first report commit (now named);
  the review ran after the PR opened, not before (disclosed, ordering kept
  because the pipeline's own review is independent of it). Lesson, third
  time this month: a timestamp match needs both sides in the same zone.
- Environment: eslint has no config in the repo, so lint is not a gate
  anywhere. Rule proposal (repeat of 09-07/09-08): a ledger-on-sight class
  match should count as a no-op with a ledger row.
- Sizes (`wc -l -c`, after the fold):
```
  106  5586 docs/loops/sentry-fix/LOOP.md
  141  9971 docs/loops/sentry-fix/LEARNINGS.md
```
