# sentry-fix run — 2026-08-25

## Issue triaged

**7690759796 / JAVASCRIPT-REMIX-63** — `Error: Ba`prod``, 1 event,
2026-08-25 04:30 UTC. Only un-ledgered unresolved issue of 14; no regressions
among ledgered issues (both `fixed` entries, #11 and #19, remain resolved in
Sentry — absent from the unresolved list).

## Root cause (from event data)

Unhandled promise rejection (`mechanism:
auto.browser.global_handlers.onunhandledrejection`, `handled: no`) captured by
our widget bundle's Sentry SDK (`sentry.javascript.react` 10.57.0) on the
turmeric blog article. The stack is
three `[native code] Promise` frames — nothing in our bundles — and the
message "Ba`prod" is a minified symbol that greps to nothing in any built
asset (`grep '\`prod' extensions/health-tool-widget/assets/*.js` → 0 matches
everywhere). Browser is `Google 359.2.735510536` (Google Search App in-app
webview) on iPhone iOS 18.3.1 — the in-app-browser injected-script class
already ledgered for 7645126135 and 7395056765.

**Verdict: not-ours.** Foreign script's rejection surfacing through our
global handler; 1 event, no user impact, nothing to fix.

## Escape analysis

No defect of ours, so nothing escaped the suite. Class learning: the
`[noise][widget]` triage prior was sharpened in place (per the dedup rule) to
cover native-frames-only rejections from in-app-browser injected scripts, so
the next one is a ledger-on-sight.

## Ledger / metrics

- ledger: 7690759796 → `not-ours` (injected-script class).
- metrics: `2026-08-25,1,0,1,0,false`.

## Data gaps

- None material: the single event's full payload was retained and sufficed;
  no external source was unreachable.

## Retro

- Charter 106 lines / 5589 B; LEARNINGS.md 80 lines / 5447 B (`wc -l -c`
  machine output; both inside caps; fleet-doctor.js not on this disk).
- Fast path cost: 2 API calls to isolate the single candidate; 2 more (event
  + breadcrumbs) plus one local grep to triage it. No workers — one issue,
  all judgment; orchestrator pulls were cheaper than briefing a worker.
- Orchestrated on Fable (top tier) as required.
- Adversarial review: pending (run truncated here if this line survives).
