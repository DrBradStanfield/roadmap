# sentry-fix run — 2026-09-07

## Issue triaged

**7716241012 / JAVASCRIPT-REMIX-6C** — `SyntheticBundleFilterProbe: Synthetic
Sentry bundle-filter verification; no user data`, 5 events, all 2026-09-07
02:30 UTC. The only un-ledgered issue among 18 unresolved (one project in
the org). No regressions: the two ledgered `fixed` issues (7425626862,
7541119772) are absent from the unresolved list, the PGRST303 family's
newest lastSeen is still 08-28 16:11Z, and the seven ledgered issues that
fired since the 09-04 run (7571150649 09-07, 7600858482 09-06, 7574701559
09-06, 7395056765 09-05, 7573442962/7573442964 09-04, 7711329041 09-04)
each match their standing rationale — theme `@shopify/events`, UpCart
JSON.parse, module-import flake, native-frame rejection, iOS 14 parseModule,
injected TV script.

## Root cause (from event data)

Not a defect. Event tags: `level=info`, `environment=bundle-filter-verification`,
`probe=candidate-filter`, `bundle=health-upload.js`; user fields all null; a
single frame named `synthetic_probe` at line 2 of the deployed upload asset.
The latest event id (`97bb2e21…`) is row three of the five-event table in
`docs/sentry-filter-verification-2026-09-07.md` on Brad's branch
`claude/sentry-bundle-filter` (PR #73, opened 02:17Z, labeled `hold`,
commit e2f2b47 at 14:34 NZ). Brad injected five synthetic exceptions, one per
deployed asset, from an isolated Chrome context to prove the candidate
`allowUrls` accepts real CDN filenames. Five events, five assets, no growth
expected.

**Verdict: wontfix (ours, expected).** Ledgered with the event-id match so a
future run never re-chases it.

## The finding that matters (Tier 0 — Brad already owns the fix)

PR #73's diff shows production `allowUrls` still names the retired
`health-tool.js`. Sentry's SDK filter has therefore been dropping every
exception raised inside `health-plan-v2.js`, its HistoryPanel chunk,
`health-upload.js` and `health-chatbot-embed.js`. Only `health-site-chat.js`
and the Pages hashed chunks get through. Consequences for this loop:

- The widget-side quiet in metrics.csv (16 of 23 runs no-op) is a lower
  bound on errors, not evidence of health. The success signal has been
  measuring a filtered stream.
- After #73 deploys, expect a burst of "new" widget issues that are really
  old. The charter's one-issue-per-run rule holds; rank by `stats` since the
  deploy date, not lifetime `count`.
- No action for this loop beyond recording it: the fix is Brad's PR, the
  file is inside the Tier 3 grant but the PR is his and on `hold`.

Undatable this run: the clone is shallow, so `git log -S` attributes the
`allowUrls` text to the graft commit (7a16b74). A full clone or Sentry's
history for `health-tool.js`-frame issues would date the blind spot.

## Escape analysis

Nothing escaped on our side. The compounding half is the `[gap][sentry]`
LEARNINGS entry above plus the sharpened `[expected][sentry]` prior:
`level=info` + a non-production `environment` tag = self-generated probe,
ledger on sight after matching event ids to the authoring branch's doc.

## Ledger / metrics

- ledger: 7716241012 → `wontfix` (synthetic PR #73 probe; ids matched).
- metrics: `2026-09-07,1,1,0,0,false`.

## Data gaps

- Blind-spot start date: shallow clone (named above).
- PR #73's two comments not read — not needed for triage; Brad's branch.

## Retro

- Charter and LEARNINGS sizes: see the machine `wc -l -c` block appended
  after the adversarial round.
- Cost: 2 probe calls + 1 latest-event pull + 1 branch diff + 1 PR read.
  No workers: one issue, all judgment; the branch diff was the whole
  investigation and cost less than a worker brief.
- Orchestrated on Fable (top tier) as required.
- Not a no-op by the charter's letter (one new issue), so a report was due.
  Worth it this time only because the probe pointed at the filter gap;
  a plain synthetic probe would be a two-line report. Proposal for Brad, no
  charter edit by me: let a self-generated probe (info level, non-production
  environment tag, ids matched to a repo doc) count as no-op with a ledger
  row and no report. Evidence: this run would have cost one ledger line.
- Adversarial review: pending at the early commit; findings folded below.
