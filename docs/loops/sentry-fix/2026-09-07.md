# sentry-fix run — 2026-09-07

## Issue triaged

**7716241012 / JAVASCRIPT-REMIX-6C** — `SyntheticBundleFilterProbe: Synthetic
Sentry bundle-filter verification; no user data`, 5 events, all 2026-09-07
02:30 UTC. The only un-ledgered issue among 18 unresolved (one project in
the org; the list is not window-limited — 08-09 and 08-19 issues are in it).
No regressions. The ledger holds six `fixed` rows: three resolved in Sentry
(7425626862, 7541119772, 7697361547), three still open but silent since
08-27, before the 980f76f deploy that regrouped them (7693185557,
7693227532, 7693659330). Four ledgered issues fired after the 09-04 run;
the reviewer pulled each latest event and the standing rationale holds:

- 7571150649 09-07 13:37Z — Chrome Mobile iOS, onerror, all frames
  `[native code]` → theme `@shopify/events`.
- 7600858482 09-06 20:12Z — Firefox 154 Windows, `/en-pl/pages/roadmap`, no
  frames → UpCart JSON.parse.
- 7574701559 09-06 18:12Z — Mobile Safari 15.6, `[native code]` fetch frames
  → module-import flake.
- 7395056765 09-05 15:14Z — Firefox iOS, single native `from` frame →
  Array.from.

## Root cause (from event data)

Not a defect. Event tags: `level=info`, `environment=bundle-filter-verification`,
`probe=candidate-filter`, `bundle=health-upload.js`; user fields all null; a
single frame `synthetic_probe` at line 2 of the deployed upload asset. UA
tags read Mobile Safari 18.0 on iPhone (emulated — the branch doc says an
isolated Chrome context). The event id `97bb2e21…` is row three of the
five-event table in `docs/sentry-filter-verification-2026-09-07.md` on
Brad's branch `claude/sentry-bundle-filter` (PR #73, opened 02:17Z, label
`hold`, head e2f2b47). Five synthetic exceptions with supplied stack URLs,
one per deployed asset. Five events, five assets, no growth expected.

**Verdict: wontfix (ours, expected).** Ledgered with the event-id match so a
future run never re-chases it.

## The finding that matters (Tier 0 — Brad already owns the fix)

PR #73's diff shows production `allowUrls` still names the retired
`health-tool.js`. The reviewer ran main's three patterns against every
deployed asset URL: the four v2 bundles and the Pages root upload bundle
DROP, `health-site-chat.js` and Pages hashed chunks PASS. It is a partial
blind spot: the SDK's `_isAllowedUrl` walks frames from the last one back,
skips `<anonymous>` and `[native code]`, and passes URL-less events — which
is why the unresolved list is native-frame noise. Consequences for this loop:

- The widget-side quiet in metrics.csv (14 of 23 runs no-op, this run
  included; machine count) is a lower bound on errors, not health.
- After #73 deploys, expect a burst of "new" widget issues that are really
  old. One issue per run still holds; rank by `stats` since the deploy date.
- #73's bot review (read by the reviewer, not by me) adds two cautions now
  in LEARNINGS: exception messages are not scrubbed and storage adapters put
  document refs in thrown messages, so read titles before quoting; and the
  probe proves filename acceptance, not live frame shape.
- No action for this loop beyond recording it: the file is in the Tier 3
  grant, but the PR is Brad's and on `hold`.

Undatable this run: the clone is shallow, so `git log -S` attributes the
`allowUrls` text to the graft commit. A full clone or Sentry's history for
`health-tool.js`-frame issues would date the blind spot.

## Escape analysis

Nothing escaped on our side. Compounding half: the `[gap][sentry]` LEARNINGS
entry plus the sharpened `[expected][sentry]` prior (`level=info` + a
non-production `environment` tag = self-generated probe; match ids to the
authoring branch's doc, then ledger on sight).

## Ledger / metrics

- ledger: 7716241012 → `wontfix` (synthetic PR #73 probe; ids matched).
- metrics: `2026-09-07,1,1,0,0,false`.

## Data gaps

- Blind-spot start date: shallow clone (above).

## Retro

- Cost: 2 probe calls, 1 event pull, 1 branch diff, 1 PR read; no workers. Fable.
- Not a no-op by the charter's letter (one new issue). Proposal for Brad, no
  charter edit by me: a self-generated probe (info level, non-production
  environment tag, ids matched to a repo doc) counts as no-op with a ledger
  row and no report. Evidence: this run's own probe would have cost one line
  — the filter-gap finding came from the branch diff, not the probe.
- Adversarial review (post-279f2bc): 7 findings, all folded. Three were
  retyped or under-evidenced numbers I claimed to have measured (16/23
  no-ops was 14/23; "two fixed rows" was six; "seven fired since 09-04" was
  four, the rest already in the 09-04 report) — the constitution's
  paste-machine-output rule exists for exactly this, and I broke it in a
  docs-only run. One was a scope error: "dropping every exception" overstated
  a partial filter. One was a data gap I had declared harmless: the unread PR
  comments held the scrub caution. Two nits (UA wording, wc block) folded.
- Sizes (`wc -l -c`, after the fold):
```
  106  5586 docs/loops/sentry-fix/LOOP.md
  122  8581 docs/loops/sentry-fix/LEARNINGS.md
```
