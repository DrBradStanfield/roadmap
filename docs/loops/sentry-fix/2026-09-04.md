# sentry-fix run — 2026-09-04

## Issue triaged

**7711329041 / JAVASCRIPT-REMIX-6A** — `TypeError: n.data.split is not a
function`, 1 event, 2026-09-04 07:25 UTC. Only un-ledgered issue among 17
unresolved; no regressions among ledgered issues (the PGRST303 family is
quiet — 7696205115 last fired 08-28 16:11Z, siblings earlier; Brad's
"resurface if still firing weeks later" condition stays unmet).

## Root cause (from event data)

Global `onerror` capture (`handled: no`) on the three-supplements blog
article. The stack is one frame: function `n`, filename AND absPath
`<anonymous>` — our five widget bundles always load from real CDN URLs, so
an anonymous-source script is eval'd/injected code. The pattern
`.data.split` (property names survive minification) greps to nothing in
`widget-src/`, `app/`, `packages/`, the 5 built assets, or the Liquid
blocks; release `health-tool-widget@177e573` = repo HEAD, so the grep runs
against the exact release that fired. Device: Samsung SMART-TV, Tizen 8.0,
Samsung Internet **7.0** — an old TV browser whose vendor/ISP overlays
inject scripts; `X.data.split(...)` is the classic shape of an injected
postMessage/WebSocket handler choking on a non-string message. Breadcrumbs
show only healthy Shopify storefront telemetry fetches.

**Verdict: not-ours** — injected-script class, third confirmation
(7690759796 "Ba`prod" 08-25, 7708790398 "Ka`prod" 09-03), new shape:
anonymous-frame onerror rather than native-frame rejection.

## Escape analysis

No defect of ours, nothing escaped. The `[noise][widget]` prior only named
the native-frames-rejection shape; sharpened in place (dedup rule) to cover
the `<anonymous>`-frame onerror shape too, so the next TV-overlay error is
ledger-on-sight.

## Ledger / metrics

- ledger: 7711329041 → `not-ours` (injected-script class, evidence in row).
- metrics: `2026-09-04,1,0,1,0,false`.

## Data gaps

- None material: single event fully retained; no source unreachable.

## Retro

- Charter 106 lines / 5586 B; LEARNINGS.md 99 lines / 6904 B (`wc -l -c`
  machine output; both inside caps; fleet-doctor.js not on this disk).
- Cost: 2 probe calls (projects + unresolved list) isolated the one
  candidate; 1 latest-event pull + local greps closed it. No workers — one
  issue, all judgment; briefing a worker would have cost more than the pulls.
- Orchestrated on Fable (top tier) as required.
- Detached-HEAD container gotcha hit again; `git checkout -B main
  origin/main` applied before any git work. The learning keeps earning.
- Adversarial review: PENDING at this early commit (truncation-proof); the
  final commit replaces this line with the review's actual outcome.
