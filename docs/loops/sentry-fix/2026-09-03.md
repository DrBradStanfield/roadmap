# sentry-fix run — 2026-09-03

## Issue triaged

**7708790398 / JAVASCRIPT-REMIX-69** — `Error: Ka`prod``, 1 event,
2026-09-03 03:55 UTC. Only un-ledgered issue among 18 unresolved; no
regressions among ledgered issues (newest `lastSeen` elsewhere is 09-03 on
7571150649, the long-ledgered Shopify-theme `@shopify/events` noise).

## Root cause (from event data)

Unhandled promise rejection (`mechanism:
auto.browser.global_handlers.onunhandledrejection`, `handled: no`) on the
reishi blog article. Stack is three `[native code] Promise` frames — nothing
in our bundles — and the message `Ka`prod` greps to nothing in any of the 5
built widget assets (release `health-tool-widget@d5916a9` = repo HEAD, so the
grep runs against the exact release that fired). Browser `Google
420.4.909430193` — the raw UA carries an explicit `GSA/420.4.909430193`
token (Google Search App in-app webview, direct evidence) — on iOS 18.3.2.

**Verdict: not-ours** — the injected-script class, byte-for-byte the pattern
of 7690759796 ("Ba`prod", 08-25). The `[noise][widget]` prior makes this a
ledger-on-sight; triaged without fan-out.

## Escape analysis

No defect of ours, nothing escaped. The 08-25 class learning did its job:
this triage cost 4 API calls and one grep. Prior sharpened in place with the
second confirmation (dedup rule), not appended.

## Ledger / metrics

- ledger: 7708790398 → `not-ours` (injected-script class).
- ledger note updated in place: 7696205115 (PGRST303 platform fault) quiet
  since 08-28 16:11Z — Brad's "resurface if still firing weeks later"
  condition is NOT met; fault appears cleared. Sibling 7697361547 now shows
  `resolved inRelease health-tool-app@84cbd65`, no events since 08-28.
- metrics: `2026-09-03,1,0,1,0,false`.

## Data gaps

- None material: single event fully retained; no source unreachable.

## Retro

- Charter 106 lines / 5586 B; LEARNINGS.md 97 lines / 6744 B (`wc -l -c`
  machine output; both inside caps; fleet-doctor.js not on this disk).
- Cost: 2 probe calls (projects + unresolved list) isolated the one
  candidate; 2 more (latest event + 7697361547 status) plus one local grep
  closed it. No workers — one issue, all judgment; briefing a worker would
  have cost more than the pulls.
- Orchestrated on Fable (top tier) as required.
- Container started on detached HEAD again — `git checkout -B main
  origin/main` applied per the standing `[gotcha][process]` learning before
  any git work; the gotcha keeps earning.
- Adversarial review (post-c24e418): verdict upheld, all substantive claims
  independently reverified. Reviewer reproduced the unresolved list (18, one
  project in the org so no blind spot), diffed all ids against the pre-run
  ledger (7708790398 the only un-ledgered), swept every lastSeen against its
  ledger note (all PGRST303 "fixed" issues last fired BEFORE their fix
  deploys; 7448979905's 08-28 activity already recorded), re-derived the
  verdict from the raw event (breadcrumbs show only healthy storefront
  fetches; closed the dynamic-message escape hatch — `prod` never occurs as
  a standalone token in any bundle, so our code cannot construct the message
  at runtime), verified the grep chain (5 assets, `git diff d5916a9 c24e418
  -- extensions/` empty), both issue statuses, CSV schemas and arithmetic,
  and both wc counts. Two findings, both folded in: the early
  truncation-proof commit carried the "review pending" marker (this
  paragraph replaces it — same miss as 08-25); the GSA claim was labeled an
  inference when the UA carries an explicit `GSA/` token (corrected above —
  the evidence was stronger than reported).
