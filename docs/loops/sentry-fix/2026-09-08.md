# sentry-fix run — 2026-09-08

## Issue triaged

**7718908397 / JAVASCRIPT-REMIX-6D** — `Error: YouTube replies list failed:
503 {…}`, 1 event, 2026-09-08 09:53 UTC. The only un-ledgered issue among 18
unresolved (one project in the org). No regressions: no issue carries
`substatus=regressed`, and the six `fixed` rows all last fired on or before
08-28. Seven ledgered issues fired after the 09-07 run's probe; each matches
its standing rationale (theme `@shopify/events` 09-07 23:47Z; UpCart
JSON.parse 09-06; module-import flake 09-06; Array.from 09-05; the parseModule
pair 09-04; `n.data.split` 09-04 — all already covered by 09-04/09-07).

## Root cause (from event data)

Server event, `platform=node`, tags `feature=youtube-bot`, `handled=yes`,
machine `e2861050f14d58`, Fly `iad`. Google's Data API answered one
`GET /youtube/v3/comments?parentId=…` with HTTP 503 and the body
`"The service is currently unavailable."`; the six sibling `listReplies`
calls in the same tick (breadcrumbs) returned 200. Stack: `listReplies`
(youtube-bot.server.ts:285) → `processFollowUps` (:947) → `tick` (:1183).

The call site is designed for this: `processFollowUps` wraps each thread's
`listReplies` in try/catch, calls `logTickError`, and `continue`s to the next
thread. `fetchRecentPostedThreads` rebuilds the thread list every tick, so the
skipped thread is re-scanned on the next half-hourly tick. The degradation
direction is "no post this tick", not a value that feeds a cap or lock
decision — nothing fails open (the PGRST303 class does not apply). The thrown
message already truncates the upstream body to `status + 200 chars`, so the
grouping problem the 08-12 learning flagged for HTML pages is absent here.

**Verdict: wontfix (ours, transient upstream, handled as designed).** Same
class as 7653667248 (Google OAuth 500, 08-05) and 7661635476 (Cloudflare
page, 08-09). Ledgered with the resurface condition: several events in one
day.

## Escape analysis

Nothing escaped — there is no defect. Compounding half: the `[noise][server]`
youtube-bot LEARNINGS entry was sharpened in place (dedup rule) so the next
single Google 5xx captured via `logTickError` is ledger-on-sight, with the
two conditions that would make one worth a look.

## Ledger / metrics

- ledger: 7718908397 → `wontfix` (evidence in row).
- metrics: `2026-09-08,1,1,0,0,false`.

## Data gaps

- Fly logs not reachable from this run: the 09:53Z tick's summary line
  (`youtube_bot_tick` with `followUpGates`) would confirm the other six
  threads completed. Breadcrumbs (six 200s before the 503) stand in for it.

## Retro

- Cost: 2 probe calls (projects + unresolved list), 1 latest-event pull, one
  file read at three frames. No workers — one issue, all judgment; briefing a
  worker would have cost more than the pulls. Orchestrated on Fable.
- Not a no-op by the charter's letter (one new issue), so a report exists.
  Restating the 09-07 proposal with today's evidence: a single handled
  upstream 5xx on the youtube-bot, matching a ledgered class, cost a report
  and a review for a one-line verdict. If Brad adopts "ledger-on-sight class
  match = no-op with a ledger row", this run and 09-07 both shrink to two
  CSV lines.
- The fast-path probe still lists all 18 unresolved issues each run (the
  `[gotcha][sentry-api]` learning: the list is not window-limited). The
  ledger diff is what makes it cheap; the probe itself is not.
- Sizes (`wc -l -c`, after the LEARNINGS edit):
```
  106  5586 docs/loops/sentry-fix/LOOP.md
  127  9031 docs/loops/sentry-fix/LEARNINGS.md
```
- Adversarial review: pending at first commit (see below; folded before push).
