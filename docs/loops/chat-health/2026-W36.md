# Chat-health — 2026-W36 (run 2026-09-05/06 NZ · window 2026-08-29T22:25Z → 2026-09-05T22:24Z, 7d)

## TL;DR

- **Brad's 2026-08-30 router-prompt fix (issue #39) verified against
  production and the harness**: KCl-brand, salt-ratio, lithium-test and
  K2-clotting (W34's claim-shape fail) all pass 3/3 now, and no
  practical-shape empty recurred in production. Still failing:
  tirzepatide-company (selection-side, as predicted).
- **Empty handles 17 (was 38) on halved traffic; zero classifier-catchable
  for the second week; zero router errors/fallbacks. But routing misses are
  now the largest real bucket: 6/17 (35%), every one consistent at baseline
  (∅ 3/3 or a 3/3 steal), none stochastic** — two Discord Lp(a) questions,
  three K2 questions on the new cardiologists-K2 video, one web
  "how do I start microdosing tirzepatide". Two are steals
  (`chronic-non-cancer-pain` — the pain family's third — and
  `hyperlipidaemia`, a first).
- **One summary fix shipped with numbers (ECA: supplement 19/20 → 20/20 on
  the original set; the fixture went ∅ 3/3 → pass 3/3) and the first
  attractor-side steal experiment was run and reverted on its numbers** —
  weakening `medications-in-chronic-pain`'s generic drug-safety vocabulary
  plus re-adding victim terms did not release the statin-cognition steal
  (3/3 before and after). 6 production fixtures added (313 → 319).
  Peptides quiet a second week.

## Empty-handle turns — 17 (7d) · category table

| Category | n | share | W35 (n=38) | Meaning |
|---|---|---|---|---|
| Opinion/reaction/anecdote (YouTube; companion blog in context) | 8 | 47% | 74% | Correctly empty |
| Routing miss (content exists) | 6 | 35% | 13% | All consistent 3/3 at baseline — see fixes section; 0 stochastic (first time) |
| In-context question (needs video/conversation context) | 3 | 18% | 13% | 2 Discord deep-mechanism follow-ups + 1 "what's a mega dose?" (no substance named) |
| Genuine content gap | 0 | 0% | 0% | second week with no new themes |
| Classifier-catchable | 0 | 0% | 0% | second consecutive zero |

Split: shopify 1 · youtube 12 · discord 4 (Discord's first empties; W34's
"quiet vs broken" answered — volume). Router errors 0, fallbacks 0. Total
match events 93 (was 224) on the YouTube ebb — matches product-health W36
independently (93 = 51 skipped + 42 ran; their "17/42" = this count). Prior
slice re-measured 38 = published (no drift).

## Latency (router ran; n=42)

| metric | W36 | W35 | Δ |
|---|---|---|---|
| median | 1,494ms | 1,322ms | +13% |
| p90 | 1,920ms | 1,861ms | +3% |
| cache hit | 31% | 27% | +15% |

Small-n (42 vs 82) wobble, all within the W32–W35 envelope; no action.

## YouTube reply length (prompt-cap check)

n=27 · median 70 words (W35: 67) · 2 >90 words (7.4%), max 123 · 0
>5-sentence breaches. Cap holding. Found in passing: 3 literal
`SKIP_NO_REPLY` sentinels stored as 1-word assistant rows dilute this
metric (median 74.5 without them); proposal #3.

## Fixes applied (with before/after)

Command per category: `npx tsx tools/test-chatbot-matching.ts --category
<cat> --runs 3 --concurrency 5`; fixtures appended and baselined first.

**E1 — `weight-loss-supplements…` summary: "ephedra/ECA stack harms and
ban" added inside the visible 150 chars. KEPT.** The ECA fixture (W34
production, passed at its own baseline then) had regressed to ∅ 3/3;
no summary named ephedra/ECA (term absence — the one failure class summary
edits fix). Before: supplement 19/23 (ECA ∅ 3/3) → after: 21/23 → final
config (E2/E3 reverted): 20/23 with ECA passing and **all 20 original
supplement fixtures passing**; the 3 fails are this week's new known-fail
fixtures.

## Fixes attempted and reverted (with numbers)

**E2+E3 (pre-registered pair) — attractor-weakening on
`medications-in-chronic-pain` ("drug selection… dosing principles, safety
considerations" → pain-anchored phrasing) + victim terms on
`the-30-year-statin-panic…` ("memory/cognitive complaints" into the visible
150).** Rationale: W33 proved victim-side alone fails; attractor+victim was
the untested combination, and the attractor now has 4 documented steals.
Result: statin-cognition still stolen 3/3 by `medications-in-chronic-pain`
(target unchanged); symptomatic 76/81 → 77/81, cardiovascular 10/14 → 10/14
(noise). **Reverted** per the rule. Side observation: the pair perturbed
unrelated selections (tirzepatide-company flipped ∅ → a
`non-insulin-diabetes-medications` steal; Lily-Johnston passed under E2/E3,
∅ again on revert) — summary edits shift boundaries index-wide (LEARNINGS).

## Routing misses confirmed this week (all fixtures added, 313 → 319)

1. **Lp(a) arterial-wall mechanism** (Discord): ∅ 3/3 — "Lp(a)" verbatim in
   the `lipoproteina` summary; pure knowledge shape. Refusal.
2. **Lp(a) diet response** (Discord follow-up): `chronic-non-cancer-pain`
   steal 3/3 in harness; ∅ in production (which had conversation context).
3. **K2 beyond-CAC variables** (YouTube, new video): `hyperlipidaemia` steal
   3/3 though "coronary calcium score" is verbatim in the new K2 summary.
4. **K2 — third-party-recommender disagreement** (YouTube): ∅ 3/3; the
   cardiologists-K2 blog IS this exact topic.
5. **Natto dose / should I stop** (YouTube): ∅ 3/3 — a should-I shape Rule 6
   explicitly routes; MK-7 dosing verbatim in the visible summary.
6. **How do I start microdosing tirzepatide** (web, "trazepatide" verbatim):
   ∅ 3/3 — the W35 tirzepatide class continues.

## Content-gap backlog

[content-backlog.csv](content-backlog.csv): peptides 0 queries a second
quiet week (7 cumulative, open). No new themes; the Lp(a) pair is a routing
miss (content exists), not a gap.

## Proposals needing Brad (Tier 0 — propose only)

1. **Router prompt, residue of the Rule 6 fix**: four shapes still refuse
   with terms visible — third-party-recommender disagreement (#4),
   should-I-stop-taking-X (#5), how-do-I-start/obtain (#6 + W35's
   tirzepatide-company), mechanism-depth (#1). All are fixtures; I'll run
   the precision check the moment any prompt edit lands, as in W35.
2. **Selection-side steals are now proven summary-immune from both sides**
   (chronic-pain family: 4 documented; hyperlipidaemia: 1st): W33 killed the
   victim-side edit, this run killed the attractor-side edit with numbers.
   The remaining levers are the router prompt (how it picks among plausible
   entries) or the model — both outside loop scope. Recommend treating
   steals as one workstream with proposal #1.
3. **Stop persisting `SKIP_NO_REPLY` as assistant content** (tiny): 3 rows
   this window store the sentinel string as a 1-word assistant message —
   pollutes reply-length metrics and the conversation record. One-line code
   fix wherever the YT bot writes the skip.
4. **Guard `file-adapter.test.ts` US-31 AC8 for root** (tiny): the chmod-
   based write-failure test fails on any uid-0 runner (root ignores 0o500) —
   fails here on a CLEAN tree. Skip-when-root guard keeps cloud `npm test`
   green and honest.

## Data gaps

- `docs/chat-overview.html` (claude_business) — standing cloud-runner gap.
- Deploy timing: this env can't see when the 08-30 prompt fix reached Fly;
  production behavior (no practical-shape empties post-08-30) is the
  verification used.
- Classifier skip quality: unchanged from W35 — only `classification` labels
  pulled; a mislabelled skip would be invisible.

## Retro

- **Worked**: verification-first agenda (Brad's fix checked against
  production traffic + all five W35 fixtures before anything else);
  baselining all 6 candidates before any edit — zero speculative edits;
  pre-registering E1/E2/E3 keep-conditions made the revert mechanical.
- **Wasteful, priced as worth it once**: E2+E3 spent ~750 harness calls on
  a measured negative that permanently closes the attractor-side question
  (LEARNINGS; not to be re-run). Environment residue handled: package-lock
  churn reverted; `npm test` red on a CLEAN tree (root runner, proposal #4).
- **Workers**: one Sonnet gather worker (9 deliverables, exact counts,
  cross-validated against product-health W36 independently). Categorisation
  and every verdict stayed with the orchestrator (Fable, no fallback).
- **Adversarial review**: [PENDING]
- Vitals (machine `wc -l -c`; fleet-doctor.js not on disk): LEARNINGS
  `141 10053` · charter `172 9412` · metrics.csv `41 5263` · this report
  below the 150 cap.
