# Content-gap backlog — cumulative (chat-health loop)

Queries real users asked that NO document covers (the "genuine content gap"
category — 19% of empty-handle turns at the 2026-08-07 baseline). The loop
updates this table every run: new themes appended, repeat themes incremented
IN PLACE (never duplicated), example queries anonymised and never containing
personal health detail. Evidence accumulates here so a theme asked about three
weeks running is visibly different from a one-off.

How Brad consumes it: the weekly Gmail draft lists the top 3 new or growing
themes with a link here; build sessions (Brad + interactive Claude) pick items
off, create the pathway/blog via the claude_business content skills, and set
Status so the loop stops re-reporting them. Rows built or declined may be
pruned after a month — git history is the archive.

| Theme | Example queries (anonymised) | 7d | Cumulative | First seen | Status |
|---|---|---|---|---|---|
| _(seeded empty — the 2026-W33 run backfills from the W32 gap category)_ | | | | | |

Status values: open · planned · built (handle) · declined (reason).
