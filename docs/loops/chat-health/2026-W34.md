# Chat-health — 2026-W34 (run 2026-08-22/23 NZ · window 2026-08-09T22:25Z → 2026-08-22T22:25Z, 13 days)

The 08-15 scheduled run never fired — this window covers two weeks. Metrics
rows use the last-7d slice; deltas use the measured prior-7d slice where available.

## TL;DR

- **Empty-handle turns 40 (7d, up from 26 prior-7d) — but the rise is one
  comment wave**: 14 of the 13-day window's 58 empties sit on the new
  Sinclair/GLP-1-blindness video (mostly reactions, 2 content-gap questions,
  the 1 router error; 08-20's day total is 17), and its companion blog is
  already in the reply context. Classifier-catchable share stays at the
  floor: **1/58 (1.7%)** — one bare fragment ("female"). Zero `.max(3)`
  rejections in 13 days (was 3/7d) — consistent with the W33 slice fix being
  live, though this env can't confirm the deploy date.
- **No summary edit shipped; both candidates died on measurement.** The ECA
  routing miss passed the harness 3/3 at baseline (production empty was
  sampling noise); the K2-clotting miss failed 3/3 both before AND after
  putting "blood clotting" in the visible summary — a new failure class:
  **claim-shaped comments don't route at all**, whatever the summaries say.
  Router-prompt proposal below; 3 fixtures added (2 router + 1 classifier).
- **Top content gap remains peptides** (7 queries cumulative, 3 this 7d — two
  of them direct questions); new one-offs (Turkesterone, CADASIL) not themed.

## Empty-handle turns — 58 (13d) · category table

| Category | n | share | W33 (n=27) | Meaning |
|---|---|---|---|---|
| Opinion/reaction/anecdote (YouTube; companion blog in context) | 37 | 64% | 52% | Correctly empty; ~11 from the 14-comment Sinclair-video wave (08-20) |
| In-context question (YouTube; needs video context to route) | 11 | 19% | 15% | Correctly empty today; the queued YT-context fix (W33 #2) is what would route these |
| Genuine content gap | 4 | 7% | 11% | peptides ×2 (theme), Turkesterone, comprehensive-plan framing (web) |
| Routing miss (content exists) | 2 | 3% | 7% | ECA (stochastic — passes harness), K2-clotting (claim-shaped — see below) |
| Correctly empty (calculation / ambiguous personalised ask) | 2 | 3% | — | iron-salt stoichiometry; "supplements from my blood data" (web) |
| Classifier-catchable | 1 | 2% | 4% | "female" — bare fragment classified ROUTE, burned a 1,106ms empty router call |
| Router error | 1 | 2% | 11% | 1 transient JSON malformation (was 3× Zod .max(3), now fixed) |

Split (13d): shopify 5 · youtube 53 · discord 0. Last-7d: 40 (shopify 4 /
youtube 36). Discord had traffic this window (3 match events; W33's 0 was
volume, not breakage). Fallback-flagged messages: 0 in 13d (also 0 in W33).

## Latency (router ran; 7d n=82)

| metric | W34 (7d) | W33 | Δ |
|---|---|---|---|
| median | 1,459ms | 1,362ms | +7% |
| p90 | 1,993ms | 2,217ms | −10% |
| cache hit | 34% | 20% | +70% |

Cache-hit recovered toward the 39% W32 baseline — W33's "YT one-shot
mix-shift" read holds. Router errors: 1 (malformed JSON from the router LLM,
single occurrence, self-healing next turn — noise, no action).

## YouTube reply length (prompt-cap check, first full window since 08-10)

7d: n=74, median 66.5 words (baseline 84), 4 breaches >90 words (5.4%),
max 123, zero >5-sentence breaches. 13d: 8/135 over 90 words. The cap is
holding better than at baseline; no decay, no action.

## Fixes applied (with before/after)

**None** — both candidates were killed by measurement, the rule working as designed:

## Fixes attempted and reverted (with numbers)

Command: `npx tsx tools/test-chatbot-matching.ts --category supplement --runs 3
--concurrency 5` (19 cases incl. the 2 new regression cases).

1. **ECA short-term-harm query** (web, 08-22): `weight-loss-supplements`
   body has a full Ephedra section; no summary names ephedrine. Baseline:
   **passes 3/3 with no edit** — the production empty was router sampling
   noise, not a summary defect. No edit made; fixture keeps it covered.
2. **`vitamin-k-benefits…` summary** — added "blood clotting (K1 and K2
   roles)" inside the visible 150 chars for the K2-clotting query (body has a
   dedicated Blood Clotting section). Before 17/19 (89.5%), K2 case ∅ 3/3 →
   after 17/19 (89.5%), K2 case still ∅ 3/3. **Reverted** (unchanged → revert).
   The router consistently returns *nothing* for this rebuttal-shaped input —
   not a wrong pick, a refusal to route. Summary terms are not the lever.

Pre-existing variance, not chased: "athlete protein target" passes 2/3 runs
before and after — unrelated to this window.

**Fixtures added to `tools/test-queries.json` (3):** ECA (supplement), K2-clotting
(supplement, known-fail), "female" (classifier-greeting, known-fail until a
prompt change). All `source: production`. `npm test`: 816 + 215 passed.

## Content-gap backlog

[content-backlog.csv](content-backlog.csv): **peptides** row updated in place —
3 queries this 7d (two direct questions: "what are compounded peptides
actually", BPC-157 dosing), 7 cumulative since 08-10, spread across three
videos. Still the only repeating theme. One-offs not themed: Turkesterone/
ecdysteroids, CADASIL/GOM, "comprehensive health plan" framing (web).

## Proposals needing Brad (Tier 0 — propose only)

1. **Router prompt: route claim-shaped inputs to the disputed topic.**
   "K2 is NOT involved in blood clotting. Show me a reference." returns ∅
   3/3 even with "blood clotting" in the visible summary — the router treats
   rebuttals/claims as non-questions. Suggested: one line in
   `chat-router-prompt.md` ("a factual claim or challenge about a topic is a
   query about that topic"). Needs a harness precision check that it doesn't
   over-fire on the 37 opinion/reaction comments — I can run that the moment
   the prompt lands on main.
2. **Classifier prompt: bare no-question fragments → GREETING.** "female"
   (web) classified ROUTE and burned a 1.1s empty router call. Tiny volume
   (1/58); bundle with any other classifier-prompt edit rather than shipping
   alone. Fixture already in the suite.
3. **Status note, no decision needed:** the queued YT video-context fix
   (W33 #2, by-videoId constraint) would address the 11 in-context questions
   (19% of this window's empties); its value keeps accruing weekly.
4. **Ops: the 08-15 scheduled run never fired** — no report, no data pull
   that week (this report's window covers both weeks). Worth checking the
   trigger/schedule; if it recurs, the loop's cadence promise is broken.

## Data gaps

- `docs/chat-overview.html` (claude_business) — standing cloud-runner gap.
- Missed 08-15 run → no adjacent-week prior slice for latency/YT-length
  metrics; deltas there compare to the W33 window instead (noted in CSV).
- This session is pinned to branch `claude/bold-allen-257lq5` by the cloud
  runner (cannot push main). Report + fixtures land there for a build
  session to merge — a delivery deviation from the constitution's
  commit-to-main rule, not a choice.

## Retro

- **Worked**: measure-first killed both edits — one stochastic (ECA), one
  structural (claim-shaped). 4 attempted content edits across two runs, 0
  shipped; the discipline has been right every time.
- **Wrong/wasteful**: one summary edit + two index rebuilds spent on a query
  the verbose-baseline already showed routing to ∅ consistently — the W33
  "inspect what the router picked instead" rule should extend to: a 3/3-∅
  claim-shaped input is router-prompt territory, don't try the summary at all.
  Charter-rule suggestion (evidence: this run): add that sentence to Verify.
- **Worker usage**: one Sonnet worker (full Supabase pull, 7 deliverables,
  exact-count verified, caught the router_context videoId field) — right
  size; categorisation and all verdicts stayed with the orchestrator.
- **Orchestrator tier**: Fable (no fallback).
- **Adversarial review**: 8 findings, all closed. Fixed by name: #1 "17 =
  Sinclair wave" was the 08-20 day total, the video cluster is 14 (TL;DR +
  table re-worded); #2 fixture count 4→3; #3 vitals re-pasted from machine
  output below; #4 router_errors_7d prior now the measured slice (1→1, 0%);
  #5 yt-median row 66.5/−21; #6 "~1.6s" was the fleet average — this call
  was 1,106ms. Rebutted: #7 the verbatim K2 fixture stays — public
  scientific claim, zero personal detail, and paraphrase would change the
  claim-shape under test (Brad may overrule); #8 softened to "consistent
  with the fix being live" (deploy date unverifiable here). Commit
  27a9d52's message still says 3+1 fixtures — pushed pre-review, not amendable.
- Vitals (raw `wc -l -c`, post-review): `150 8825` this report · `117 8280`
  LEARNINGS.md · `172 9412` LOOP.md (fleet-doctor.js not on disk here).
