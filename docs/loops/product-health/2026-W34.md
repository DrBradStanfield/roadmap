# Product-health — 2026-W34 (run 2026-08-22/23 NZ · window 2026-08-15T20:47Z → 2026-08-22T20:47Z)

First clean week-over-week compare (both windows fully instrumented). Workflow
integrity: clean — the only non-Brad workflows commit in the 8d lookback is
`2d5b372`, already acked by Brad live in the W33 run (issue #25).

## TL;DR

- **`chat_opened` is ALIVE: 0 → 29.** The first event ever fired at
  2026-08-15T22:52:36Z — ~3 minutes after PR #23's deploy (run 31911537298)
  finished at 22:49:50Z. The two-week instrumentation saga is closed, and this
  is the production-telemetry live-verify that W33's egress-blocked retro item
  owed. Chat itself grew again: 335 messages (+29%), 222 user-authored.
- **Retention engine week 2 is accelerating**: 38 enrolments (+111%), 0
  opt-outs ever (kill-ratio 0/56 vs the 30% threshold), 35 plan-ready sends,
  21 click events (**60% click:send event ratio**, up from a like-for-like
  40% — not a per-recipient CTR; backlog #1 rider), 0 bounces, 0 complaints. All-time
  rows 58 = 52 typed + 6 google-drive (~9:1 — the US-23 bet keeps holding).
  Cloud connects had their best week ever: 5 successes (vs 3 all-time before).
- **A scraper wave has drowned drstanfield.com's Clarity**: 357k "sessions"/3d
  (~79× normal), 99% one fingerprint (Chrome + macOS + PC + no referrer),
  ~22s engagement, spread US/BR/VN/MX — synthetic, not viral. Human web-side
  trends are unreadable this week (named gap); the Supabase widget funnel is
  untouched (results_viewed flat at 352). Worth Brad checking Shopify
  bandwidth/bot settings — ~119k JS-executing hits/day is real load.

## What changed per source

- **Supabase — funnel**: results_viewed 352 (flat, ~50/day). Uploads 6→5
  saved — the first sub-100% completion week; `upload_extract_failed` is 0, so
  the one drop happened at/around review, not extraction (small n; watch).
  Corrections 9 (steady). lab_rows_viewed 9 (~1.3/day, stabilized) and
  **lab_row_added fired its first 3 events ever** — US-21 phase 2 has real
  usage. W33's relayout-vs-novelty question resolves as novelty wear-off; the
  08-14 relayout is not implicated (usage recovered, not collapsed).
- **Supabase — reminders/email (US-17/22/23)**: 38 optins (35 typed, 3 drive),
  0 optouts, 35 sent / 21 clicked / 0 bounced / 0 complained. The flywheel
  guess worth watching: email re-entry may be driving the cloud-connect uptick.
- **Supabase — chat**: 335 messages; match events 222, of which 42 matched,
  140 deliberate `router_skipped`, leaving TRUE router misses **40/82 = 49%**
  — back at the ~48% audit baseline after W33's 35%. One good week was not a
  trend; chat-health's lane (cross-loop flag, not ours to fix).
- **Supabase — A/B**: Subheading test `completed` 2026-08-16 (Brad shipped
  variant A same day as the W33 report — issue #25). Impressions frozen at
  A 5,290 / B 5,691; conversions ticked to 248/222 (late attribution only).
- **Sentry (14d)**: 1 new issue — JAVASCRIPT-REMIX-62 "Invalid URL. Refusing
  to redirect" (node, culprit `GET *`, 2 events, first seen 08-22T20:12). The
  string is not in our code — a dependency-level redirect guard on a server
  route, bot-shaped junk; watch only. Chat fixes hold: -1M quiet since 08-07,
  -3X quiet since 08-14 (its last event predates the 08-15 resolve/deploy —
  not a regression). Theme parseModule/`@shopify/events` cluster still firing
  (lastSeen today; audit #9 unresolved). userCount=0 on all 20 issues — tags
  still missing. `Chat listConversations failed` is no longer findable in
  Sentry under any search (merged or deleted; soft gap).
- **Clarity (3d)**: microvitamin normal — 2,785 sessions (+29% vs prior pull),
  script errors 4.78% (flat, consistent with the theme cluster). drstanfield:
  see TL;DR — pull repeated twice, near-identical (~0.02% live-traffic drift),
  so it is real crawler traffic hitting Clarity, not an API mis-scope.
- **Gmail**: no feedback emails in 8d. `feedback_submissions`: 0 in window.

## Funnel (7d vs prior 7d — rows appended to metrics.csv)

| step | W34 | W33 | note |
|---|---|---|---|
| results_viewed | 352 | 351 | flat; crawler flood absent from widget funnel |
| upload_started → saved | 6 → 5 | 2 → 2 | first sub-100%; drop at review, not extraction |
| cloud_connect started → success | 6 → 5 | 3 → 1 | best week ever; cumulative 14 → 8 |
| chat_opened | **29** | 0 | LIVE — first event 3 min post-deploy |
| correction_made | 9 | 11 | steady |
| lab_rows_viewed / lab_row_added | 9 / **3** | 6 / 0 | first-ever manual adds (US-21 ph2) |
| reminder_optin / optout | 38 / 0 | 18 / 0 | kill-ratio 0/56 cumulative |
| report_email sent / clicked / bounced | 35 / 21 / 0 | 25 / 10 / 1 | 60% CTR |
| chat_messages (context) | 335 | 260 | +29%; 222 user-authored |

Zero-classification (charter rule): `upload_extract_failed`,
`reminder_optout`, `report_email_bounced`, `report_email_complained` — all
true zeros. Emit sites/webhook verified live in W32/W33 runs — except
`report_email_complained`, verified only by inheritance from the shared
bounce webhook (it has never fired).

## New errors

Only JAVASCRIPT-REMIX-62 (above): dependency-level redirect guard, server
route, 2 events, consistent with bot junk. Nothing user-facing.

## New feedback

None organic (Gmail 0 in 8d; 0 `feedback_submissions` in window).

## Proposed backlog (build sessions decide)

1. **Sentry user/surface tags** — audit #9, US-15; third carry, now the top
   item: chat adoption is finally measurable (29 opens/wk) and email volume
   is compounding, yet every issue reads userCount=0 and the theme cluster
   still can't be triaged by surface. Effort S (server-side — outside this
   loop's Tier 3 area, so it stays a proposal). Rider (review catch): email
   `product_events` carry one constant visitor_id and empty metadata — add a
   per-recipient dimension so click:send becomes a real CTR.
2. **Scraper-wave check (ops; no covering US — analytics/infra)**: ~119k
   JS-executing sessions/day on drstanfield.com. Check Shopify bandwidth/bot
   management and consider blocking; Clarity is unusable for human trends
   while it lasts. Effort: Brad ops call, XS to look.
3. **Router true-miss rate back to ~49%** — US-15, cross-loop flag to
   chat-health: W33's 35% was one week, not a trend; message volume +29%.
4. **Cloud-connect failure-step metadata** — US-09, carried but demoted: 5/6
   succeeded this week, so the drop point is currently cheap to ignore; still
   blind if the rate reverts.
5. **Watch upload review-abandon** — US-12/13: first started→saved gap (5/6)
   with zero extraction failures. One user, small n; if it repeats, propose a
   review-abandon event before guessing at causes.

## Data gaps

- Clarity drstanfield: sample unusable (crawler flood) — the
  `clarity_roadmap_visits_3d` metric row is a named gap this week, not a 0.
- Sentry per-issue counts remain soft (standing, LEARNINGS); the
  `listConversations` issue title is unfindable, so its status is unverifiable.
- `product_events` still lacks a surface/store dimension (carried).
- No unreachable sources: Supabase, Sentry, Clarity ×2, Gmail all answered.

## Retro

- **Orchestrator**: Fable, no fallback. 3 Sonnet workers + inline Gmail/git/
  GitHub, all first-pass. Two worker catches earned their cost: the Supabase
  worker found PostgREST's silent 1000-row page cap returning *different*
  arbitrary subsets on identical unordered queries (ab_events > 1000 rows) and
  self-corrected via exact-count + ordered pagination; the Clarity worker
  refused to report the drstanfield numbers as a trend and flagged the anomaly
  that became the week's third headline. Both promoted to LEARNINGS.
- **Success signal (charter)**: W33 backlog #1 shipped and is now
  production-verified by this run's own telemetry (chat_opened 0→29); #2 was
  decided and shipped by Brad the same day the W33 report landed (issue #25
  closed, variant A static). Two of five items picked up within a day.
- **Tier 3 grant**: not exercised (0 of 1) — no defect this week sits in the
  granted area (`widget-src/**`/health-core) with a failing-test story; the
  top backlog item is server-side and stays a proposal.
- **Adversarial run-review (constitution)**: every reported number recomputed
  exactly from source (Supabase exact-counts, Clarity raw JSONs, Sentry live
  API, ab_tests, CSV delta arithmetic, write-scope diff line-by-line, zero
  HTML regeneration drift; per-event W34 counts sum to exactly 518). Fixed by
  name: **#1** "60% CTR" was an event ratio — sends/clicks carry one constant
  visitor_id and no metadata, so 21 clicks ≠ 21 recipients (re-worded
  everywhere; per-recipient dimension → backlog #1 rider; trend stands);
  **#2** the two Clarity pulls were
  near-identical (~0.02% live drift), not literally identical — re-worded;
  **#3** `report_email_complained`'s "verified live" was inherited from the
  shared bounce webhook, never observed — classification stands, citation
  weakened. Rebutted: none.
- Vitals (raw `wc -l -c`, pre-amendment):
  `80 4772 docs/loops/product-health/LOOP.md` ·
  `66 4608 docs/loops/product-health/LEARNINGS.md` ·
  `63 6945 docs/loops/product-health/metrics.csv`.
