# Product-health — 2026-W35 (run 2026-08-29/30 NZ · window 2026-08-22T20:47Z → 2026-08-29T20:47Z)

Workflow integrity clean: every in-window `.github/workflows/` commit is Brad's
own (loop-issue-notify hardening 08-24 ×2, stranded-branch watchdog 08-29).

## TL;DR

- **Reach dipped for the first time: results_viewed 352 → 261 (-26%)**, ~50/day
  → ~37/day, after three flat weeks. Engaged usage held (chat_opened 30,
  corrections 11 (+22%), uploads 6, lab rows steady), and the capture layer
  moved with reach, not worse than it (optins 31, plan-ready sends 29, both
  ~-18%). The one upstream lens is blind: Clarity drstanfield is still
  scraper-flooded, so blog/roadmap page traffic is unreadable — the dip cannot
  yet be split into "fewer visitors" vs "same visitors, fewer plan views".
- **Retention engine week 3: still zero opt-outs.** 31 enrolments; kill-ratio
  0/87 cumulative events vs the 30% threshold; rows 89 all-time (81 typed +
  8 drive — the ~10:1 US-23 bet still holding). 0 bounces, 0 complaints.
  Click events 10 vs 29 sends (34% event ratio, down from 60% — uncohorted
  and un-recipient-dimensioned, so treat as drift, not a verdict).
- **The week's only new Sentry issues are the PGRST303 Supabase platform-fault
  class (5 issues), already root-caused and hardened by the sentry-fix loop**
  (Tier 3 PRs #30 + #35, deployed 08-27/28; quiet since 08-28T16:11Z). Its
  product residue is two new ops-safety stories, US-27/US-28. Reminders were
  never at user-visible risk (nothing due until 2027).

## What changed per source

- **Supabase — funnel**: results_viewed 261 (-26%, first real decline).
  Uploads 6 started → 5 saved, and the gap is EXPLAINED this week: 1
  `upload_extract_failed` — the event's first-ever fire (US-12 AC3 emit
  verified in production; W34's review-abandon watch does not trigger).
  Corrections 11 (+22%). lab_rows_viewed 8 / lab_row_added 2 (steady ~1/day
  post-novelty). Cloud connects 5 → 3 (cumulative 19 → 11, ~58% all-time).
- **Supabase — reminders/email (US-17/22/23)**: 31 optins (typed-dominant),
  0 optouts, 29 sent / 10 clicked / 0 bounced / 0 complained. All zeros are
  true zeros (emit sites/webhook verified live W32–W34; `complained` still
  by inheritance from the shared webhook — it has never fired).
- **Supabase — chat**: 331 messages (flat; 224 user-authored). Match events
  224: 44 matched, 142 deliberate `router_skipped`, TRUE misses 38/82 = 46%.
  Platform split (the W34 learning, now standard): YouTube bot 194/224 (87%);
  web-chat only 30 events — 6 matched, 21 skipped, **3 true misses** (miss
  rate 3/9 at tiny n). `router_error` 0/224 — a clean week (the column
  populated in W34, so the path works). Chat-health's lane.
- **Sentry (14d)**: 6 new issues in-window — 5 are the PGRST303 class
  (REMIX-64/65/66/67/68: YouTube-bot cap reads + cron locks erroring on a
  Supabase-side "JWT issued at future" fault from 08-26). sentry-fix shipped
  fail-closed caps (PR #30, US-27) and throw-don't-skip cron locks (PR #35,
  US-28); whole class quiet ~22h at pull time (soft — Supabase fault is
  theirs, regrouping expected; Brad closed decision issue #32 as
  leave-to-Supabase). 6th: REMIX-63 "Ba`prod", 1 event, opaque minified
  frame, watch only. REMIX-62 (W34's redirect guard) silent since 08-22.
  parseModule sub-cluster (4K/4J) quiet exactly 7d; REMIX-4E is the one
  theme-cluster component still firing (lastSeen run-day). REMIX-22 "Chat
  listConversations failed" — W34's unfindable issue — is findable again and
  active (lastSeen 08-28; sentry-fix triaged a 429 blip). userCount=0 on all
  20 issues (audit #9, still).
- **Clarity (3d)**: drstanfield still flooded — 200,581 sessions/3d, ~98% the
  same Chrome+macOS+PC+no-referrer ~22s fingerprint (was 357k; volume -44%
  but shape identical; country mix shifted — Singapore now #2 at 5.5%).
  Roadmap-page visits unextractable (top-10 pages all blog posts). Second
  consecutive named gap. microvitamin organic and normal: 3,270 sessions
  (+17%), script errors 4.26% (flat, theme cluster), dead clicks 5.76%.
- **Gmail**: no feedback emails in 8d. `feedback_submissions`: 0 in window.

## Funnel (7d vs prior 7d — rows appended to metrics.csv)

| step | W35 | W34 | note |
|---|---|---|---|
| results_viewed | 261 | 352 | **-26%, first decline**; upstream unreadable (crawler flood) |
| upload_started → saved | 6 → 5 | 6 → 5 | gap = 1 extract-fail (event's first fire; not review-abandon) |
| cloud_connect started → success | 5 → 3 | 6 → 5 | cumulative 19 → 11 |
| chat_opened | 30 | 29 | steady in week 2 of measurement |
| correction_made | 11 | 9 | +22% |
| lab_rows_viewed / lab_row_added | 8 / 2 | 9 / 3 | stable ~1.1/day + steady trickle of adds |
| reminder_optin / optout | 31 / 0 | 38 / 0 | kill-ratio 0/87 cumulative |
| report_email sent / clicked / bounced | 29 / 10 / 0 | 35 / 21 / 0 | 34% click:send event ratio (uncohorted) |
| chat_messages (context) | 331 | 335 | flat; 224 user-authored |

Zero-classification (charter rule): `reminder_optout`, `report_email_bounced`,
`report_email_complained`, `router_error` — all true zeros (emit/webhook
verified live in prior runs; `complained` by inheritance, never fired;
`router_error` populated as recently as W34).

## New errors

PGRST303 class (5 issues) — platform fault, mitigated, quiet (above).
REMIX-63 (1 event, minified, opaque) — watch. Nothing new is user-facing.

## New feedback

None organic (Gmail 0 in 8d; 0 `feedback_submissions` in window).

## Proposed backlog (build sessions decide)

1. **Sentry user/surface tags** — audit #9, US-15; fourth carry, still top.
   userCount=0 on all 20 issues while chat (30 opens/wk) and email volume
   compound; the PGRST303 week showed impact triage is still guesswork.
   Effort S, server-side (outside this loop's Tier 3 area). Rider: per-recipient
   dimension on email events so click:send becomes a real CTR — this week's
   60%→34% swing is uninterpretable without it.
2. **Diagnose the -26% reach dip — needs an upstream traffic source** (US-07
   evidence / funnel health; carried scraper item W34 #2 now blocks it).
   Options: Brad checks Shopify analytics for roadmap-page sessions this week
   vs last, and/or Shopify bot management to end the crawler flood (still
   ~67k JS-executing hits/day). Until one lands, reach dips are undiagnosable.
   Effort XS-S, Brad ops call.
3. **Cloud-connect failure-step metadata** — US-09, carried: 2 of 5 attempts
   dropped this week, cumulative 19→11; the drop step is still invisible.
   Effort S (widget-side; a candidate for a future Tier 3 run if a US-09 AC
   is written for it — today it lacks one, so proposal).
4. **Cross-loop flag to chat-health**: web-chat router sample now tiny
   (30 events/wk, 87% of volume is the YouTube bot) — the weekly miss-rate
   is no longer meaningful at web-chat n; suggest trending it 4-weekly or on
   the bot-excluded stream only. `router_error` 0/224 this week.

## Data gaps

- Clarity drstanfield flooded (week 2) — `clarity_roadmap_visits_3d` is a
  named gap again, and the reach-dip diagnosis is blocked on it (backlog #2).
- `product_events` still lacks surface/store + per-recipient dimensions.
- Sentry counts remain soft (standing); PGRST303 "quiet 22h" is under a day.
- No unreachable sources: Supabase, Sentry, Clarity ×2, Gmail all answered.

## Retro

- **Orchestrator**: Fable, no fallback. 3 Sonnet workers + inline Gmail/git,
  all first-pass; the Supabase worker's platform segmentation and
  exact-count reconciliation (402+518=920 cross-check) applied both W34
  learnings unprompted. Session started on a detached HEAD — caught by the
  charter-ordered landing discipline before any commit (the exact stranding
  class Brad's 08-29 watchdog addresses; no work was at risk).
- **Success signal**: W34 backlog #2 (scraper) not yet acted on — now carried
  and blocking (backlog #2). #1 (Sentry tags) unactioned, fourth carry. The
  week's regression catch is the -26% reach dip, reported before any human
  report would have (Clarity is unreadable).
- **Tier 3 grant**: not exercised (0 of 1) — no in-scope defect: the reach
  dip is traffic-level, the extract-fail is server-side and by-design, and
  nothing in widget-src/health-core misbehaved.
- **Adversarial run-review**: see below (appended after review).
- Vitals (raw `wc -l -c`): `80 4772` LOOP.md · `81 5734` LEARNINGS.md ·
  `85 9779` metrics.csv.
