# Product-health — 2026-W35 (run 2026-08-29/30 NZ · window 2026-08-22T20:47Z → 2026-08-29T20:47Z)

Workflow integrity clean — three in-window `.github/workflows/` commits, all
Brad's own (verified with explicit ISO dates: the charter's literal
`--since=8d` is defective — approxidate reads it as "August 8", and in a
month's first days it returns EMPTY, a falsely-clean tripwire; fix in retro).

## TL;DR

- **Reach dipped for the first time: results_viewed 352 → 261 (-26%)**, ~50/day
  → ~37/day, after three flat weeks. Engaged usage held (chat_opened 30,
  corrections 11 (+22%), uploads 6, lab rows steady), and the capture layer
  moved with reach, not worse than it (optins 31, plan-ready sends 29, both
  ~-18%). The one upstream lens is blind: Clarity drstanfield is still
  scraper-flooded, so blog/roadmap page traffic is unreadable — the dip cannot
  yet be split into "fewer visitors" vs "same visitors, fewer plan views".
- **Retention engine week 3 — and its FIRST GENUINE REMINDER SEND.** 31
  enrolments, 0 opt-outs ever (0/87 vs the 30% threshold); rows 89 all-time
  (81 typed + 8 drive, ~10:1). A row enrolled 08-25 with an item already due
  08-01 carries `last_sent` 2026-08-28 — US-17's owed live-verify moment,
  evidenced by the cron's stamp (delivery itself not directly observed).
  Click events 10 vs 29 sends (34%, down from 60% — uncohorted, drift not
  verdict). 0 bounces/complaints in-window.
- **The only new Sentry issues are the PGRST303 Supabase platform-fault class
  (5 issues), already root-caused and hardened by sentry-fix** (Tier 3 PRs
  #30 + #35, deployed 08-27/28; quiet since 08-28T16:11Z, ~29h at pull). The
  risk was real: that first-ever reminder send came due DURING the fault
  window in which crons silently day-skipped — it went out 08-28, the day
  PR #35 landed (review catch: the draft's "nothing due until 2027" was
  stale — live rows hold four 2026 due dates). Residue: US-27/US-28.

## What changed per source

- **Supabase — funnel**: results_viewed 261 (-26%, first real decline).
  Uploads 6 started → 5 saved, and the gap is EXPLAINED this week: 1
  `upload_extract_failed` — the event's first-ever fire (US-12 AC3 emit
  production-verified; the failing visitor never saved, the other 5 did —
  W34's review-abandon watch does not trigger). Corrections 11 (+22%).
  lab_rows_viewed 8 / lab_row_added 2 (steady ~1/day post-novelty). Cloud
  connects 5 → 3 (cumulative 19 → 11, ~58% all-time).
- **Supabase — reminders/email (US-17/22/23)**: 31 optins (typed-dominant),
  0 optouts, 29 sent / 10 clicked / 0 bounced / 0 complained (1 bounce
  all-time, W33). First real reminder send: TL;DR. All zeros are true zeros
  (emit sites/webhook verified live W32–W34; `complained` still by
  inheritance from the shared webhook — it has never fired).
- **Supabase — chat**: 331 messages (flat; 224 user-authored). Match events
  224: 44 matched, 142 deliberate `router_skipped`, TRUE misses 38/82 = 46%.
  Platform split (the W34 learning, now standard): YouTube bot 194/224 (87%);
  web-chat only 30 events — 6 matched, 21 skipped, **3 true misses** (miss
  rate 3/9 at tiny n). `router_error` 0/224 — a clean week (the column
  populated in W34, so the path works). Chat-health's lane.
- **Sentry (14d)**: 6 new issues in-window — 5 are the PGRST303 class
  (REMIX-64/65/66/67/68: YouTube-bot cap reads + cron locks erroring on a
  Supabase-side "JWT issued at future" fault from 08-26); sentry-fix shipped
  fail-closed caps (PR #30, US-27) + throw-don't-skip cron locks (PR #35,
  US-28); Brad closed decision issue #32 as leave-to-Supabase (regrouping
  expected). 6th: REMIX-63 "Ba`prod", 1 event, minified, watch only.
  REMIX-62 silent since 08-22T21:51Z (64 min inside this window).
  parseModule (4K/4J) quiet exactly 7d; REMIX-4E still firing (lastSeen
  run-day). REMIX-22 "Chat listConversations failed" — W34's unfindable
  issue — findable and active again (lastSeen 08-28; sentry-fix triaged a
  429 blip). userCount=0 on all 20 (audit #9, still).
- **Clarity (3d)**: drstanfield still flooded — 200,581 sessions/3d, ~98% the
  same Chrome+macOS+PC+no-referrer ~22s fingerprint (was 357k; volume -44%
  but shape identical; country mix shifted — Singapore now #2 at 5.5%).
  Roadmap-page visits unextractable (top-10 pages all blog posts). Second
  consecutive named gap. microvitamin organic and normal: 3,270 sessions
  (+17%), script errors 4.26% (flat, theme cluster), dead clicks 5.76%.
- **Gmail**: no feedback emails in 8d. `feedback_submissions`: 0 in window.

## Funnel (7d vs prior 7d — rows appended to metrics.csv)

| step | W35 | W34 | note |
|---|---|---|---|
| results_viewed | 261 | 352 | **-26%, first decline**; upstream unreadable (crawler flood) |
| upload_started → saved | 6 → 5 | 6 → 5 | gap = 1 extract-fail (event's first fire; not review-abandon) |
| cloud_connect started → success | 5 → 3 | 6 → 5 | cumulative 19 → 11 |
| chat_opened | 30 | 29 | steady in week 2 of measurement |
| correction_made | 11 | 9 | +22% |
| lab_rows_viewed / lab_row_added | 8 / 2 | 9 / 3 | stable ~1.1/day + steady trickle of adds |
| reminder_optin / optout | 31 / 0 | 38 / 0 | kill-ratio 0/87 cumulative; FIRST real send 08-28 |
| report_email sent / clicked / bounced | 29 / 10 / 0 | 35 / 21 / 0 | 34% click:send event ratio (uncohorted) |
| chat_messages (context) | 331 | 335 | flat; 224 user-authored |

Zero-classification (charter rule): `reminder_optout`, `report_email_bounced`,
`report_email_complained`, `router_error` — all true zeros (emit/webhook
verified live in prior runs; `complained` by inheritance, never fired;
`router_error` populated as recently as W34).

## New errors

PGRST303 class (5 issues) — platform fault, mitigated, quiet (above).
REMIX-63 (1 event, minified, opaque) — watch. Nothing new is user-facing.

## New feedback

None organic (Gmail 0 in 8d; 0 `feedback_submissions` in window).

## Proposed backlog (build sessions decide)

1. **Sentry user/surface tags** — audit #9, US-15; fourth carry, still top:
   userCount=0 on all 20 issues while chat (30 opens/wk) and email volume
   compound. Effort S, server-side (outside this loop's Tier 3 area). Rider:
   per-recipient dimension on email events so click:send becomes a real CTR —
   this week's 60%→34% swing is uninterpretable without it.
2. **Diagnose the -26% reach dip — needs an upstream traffic source** (US-07
   evidence / funnel health; the carried scraper item, W34 #2, now blocks
   it). Brad checks Shopify analytics for roadmap-page sessions week-over-week
   and/or Shopify bot management to end the crawler flood (still ~67k
   JS-executing hits/day). Effort XS-S, Brad ops call.
3. **Reminder-send observability** — US-17: the first real send is evidenced
   only by a `last_sent` stamp; no send event or log row exists. Now that
   sends have begun, a `reminder_sent` event (or `reminder_log` write) makes
   the retention engine's core act visible. Effort S, server-side, proposal.
4. **Cloud-connect failure-step metadata** — US-09, carried: 2 of 5 attempts
   dropped this week, cumulative 19→11; the drop step is still invisible.
5. **Cross-loop flag to chat-health**: web-chat router sample now tiny
   (30 events/wk; 87% of volume is the YouTube bot) — weekly web-chat miss
   rates are noise at this n; trend 4-weekly or bot-excluded.

## Data gaps

- Clarity drstanfield flooded (week 2) — `clarity_roadmap_visits_3d` is a
  named gap again, and the reach-dip diagnosis is blocked on it (backlog #2).
- Reminder sends have no event/log trail (backlog #3) — the first send is
  inferred from its cron stamp, not observed.
- `product_events` still lacks surface/store + per-recipient dimensions.
- Sentry counts remain soft (standing); PGRST303 quiet is ~29h, barely a day.
- No unreachable sources: Supabase, Sentry, Clarity ×2, Gmail all answered.

## Retro

- **Orchestrator**: Fable, no fallback. 3 Sonnet gather-workers + inline
  Gmail/git, all first-pass; the Supabase worker applied both W34 learnings
  (platform segmentation, exact-count reconciliation) unprompted. Session
  began on a detached HEAD — caught before any commit (the stranding class
  Brad's 08-29 watchdog addresses).
- **Adversarial run-review** (same-tier, refute-briefed; every Supabase number
  independently recomputed, Clarity JSONs re-read, fresh Sentry pull
  identical, write-scope diff-checked, HTML regen byte-identical). Fixed by
  name: **#1 (HIGH)** "nothing due until 2027" was false — four live rows
  hold 2026 due dates and the first real send happened 08-28 inside the
  cron-fault window (TL;DR rewritten; US-17 evidence corrected; backlog #3 +
  LEARNINGS added); **#2** US-23 evidence said "0 bounces ever" — 1 all-time
  (W33), corrected; **#3** `--since=8d` approxidate defect (header +
  LEARNINGS + charter fix below); **#4** "quiet ~22h" mis-derived → ~29h;
  **#5** vitals now pasted machine output; **#6** REMIX-62 wording tightened.
  Rebutted: none. (Post-review, the 150-line cap was breached and re-trimmed.)
- **Charter fix for Brad**: replace `--since=8d` with `--since="8 days ago"`
  in Gather — this run the literal command over-scanned to Aug 8, and any run
  on the 1st–7th would get an empty, falsely-clean integrity check.
- **Success signal**: W34 #1 (Sentry tags) unactioned, fourth carry; W34 #2
  (scraper) unactioned and now blocking diagnosis. This week's charter-named
  catch: the -26% reach dip, plus the false "nothing at risk" reminder claim
  corrected before Brad read it.
- **Tier 3 grant**: not exercised (0 of 1) — no in-scope defect: the reach
  dip is traffic-level; extract-fail is by-design; backlog #3 is server-side.
- Vitals (machine output): `80 4772` LOOP.md · `95 6761` LEARNINGS.md ·
  `85 9777` metrics.csv.
