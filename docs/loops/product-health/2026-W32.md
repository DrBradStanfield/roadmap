# Product-health — 2026-W32 (run 2026-08-10 NZ · window 2026-08-02T20:47Z → 2026-08-09T20:47Z)

First run under the fleet constitution. Prior baseline: [usage-audit-2026-08.md](../../usage-audit-2026-08.md).

## TL;DR

- **Chat usage ~5×'d (32 → 158 messages) but `chat_opened` has emitted ZERO events ever — the instrumentation is dead on every live surface** (both FABs mount ChatSection `startExpanded`, so the only emit site, `handleExpand`, is unreachable). Ready-to-apply diff below. Router unmatched share also spiked: 73% (68/93) vs the ~48% audit baseline → chat-health loop's lane.
- **First funnel week is in** (partial — events live 08-06): 190 results_viewed · 7 upload_started → 7 saved (100%) · 5 cloud connects started → 2 succeeded · 9 corrections · 15 lab_rows_viewed · 0 reminder_optin. `/pages/roadmap` is now drstanfield.com's **#3 page** (216 visits/3d, Clarity).
- Integrity clean (all 9 workflow commits are Brad's), no organic feedback, 2 new Sentry issues (both YouTube-bot server errors, not user-facing).

## What changed per source

- **Supabase**: chat_messages 158 vs 32 (+394%); chat_match_events 93 vs 16, unmatched 68/93 = 73.1%. `product_events` total 235 all-time (table starts 2026-08-06 — W32 is a ~4-day partial week; prior weeks are no-data, not zero). `feedback_submissions`: 2 rows, both self-labeled pipeline diagnostics, no organic feedback. `reminder_optin_v2`: still 2 rows all-time.
- **Sentry** (14d): 2 new issues — `logTickError` (08-09) and `YouTube OAuth refresh failed: 500` (08-05), both platform-bot server errors, 1 event each. Big movers: theme-owned `@shopify/events`/`parseModule` cluster (104+66+61 — audit #9, still unignored), **`Chat listConversations failed` 86 events + `Chat sendMessage failed` 24 events**. `userCount=0 on every issue` — user/surface tags (audit #9) still missing, so impact is unmeasurable. Handoff: the new daily sentry-fix loop owns fixes here; flagged for it.
- **Clarity** (3-day sample): drstanfield.com 4,140 sessions; roadmap #3 page at 216 visits; dead-clicks site-wide 10.0% of sessions. microvitamin.com: 1,843 sessions, script errors elevated at 4.3% of sessions (consistent with the theme-owned Sentry cluster). Trends belong to metrics.csv, not this pull.
- **Gmail**: no feedback emails in 8d.
- **Workflow integrity**: `git log --since=8d -- .github/workflows/` → 9 commits, all `Brad Stanfield|bradstanfield@Brads-iMac.local`. Clean. (The week's repo activity was almost entirely fleet/deploy-pipeline infra + one blog post — no widget-facing changes shipped.)

## Funnel (7d vs prior 7d — rows appended to metrics.csv)

| step | W32 | prior | note |
|---|---|---|---|
| results_viewed | 190 | 0* | *events live 08-06; partial week |
| upload_started → upload_saved | 7 → 7 | 0* | 100% completion |
| cloud_connect_started → success | 5 → 2 | 0* | 40%; small n — see backlog #4 |
| chat_opened | 0 | 0 | **dead instrumentation** (backlog #1) |
| correction_made | 9 | 0* | FHIR correction has real usage (US-04) |
| lab_rows_viewed / lab_row_added | 15 / 0 | 0* | US-21 phase-1 signal live; phase 2 unbuilt |
| reminder_optin | 0 | 0 | event never fired; table still at 2 all-time |
| chat_messages (context) | 158 | 32 | +394% |

## Proposed backlog (build sessions decide)

1. **Fix dead `chat_opened` instrumentation** — US-15 AC2. Evidence: 158 messages this week, 0 events ever. Root cause: `ChatSection.tsx` emits only in `handleExpand` (collapsed→expand click), but HealthTool's desktop FAB (`HealthTool.tsx:1408`) and the site-chat FAB (`site-chat.tsx:141`) both mount `<ChatSection startExpanded>`, so no live surface ever runs it. Effort: XS. Ready-to-apply diff (also covers the collapsed path unchanged):
   ```diff
   --- a/widget-src/src/components/ChatSection.tsx
   +++ b/widget-src/src/components/ChatSection.tsx
   @@ -52,6 +52,7 @@
      // Load on mount when startExpanded (mirrors original lazy-load behavior)
      useEffect(() => {
        if (startExpanded) {
   +      trackProductEvent('chat_opened');
          actions.loadConversationsIfNeeded();
        }
      }, [startExpanded, actions.loadConversationsIfNeeded]);
   ```
   Not shipped as the Tier 3 PR because the Tier-3 ladder requires a failing test first and ChatSection has no component-test infra (consciously deferred, user-stories coverage notes) — a build session can ship this in minutes. The chatbot-embed surface (`chatbot-embed.tsx`, own UI, microvitamin `/pages/health-plan`) stays uninstrumented either way — decide if it should count.
2. **Chat reliability + Sentry tags** — US-15 / audit #9. `Chat listConversations failed` (86/14d) and `sendMessage failed` (24/14d) while chat volume quadrupled; every issue shows userCount=0 so real impact is invisible. Add anonymous user + surface tags, then diagnose listConversations. Effort: S–M. (sentry-fix loop may pick up the diagnosis; the tags decision is a build-session call.)
3. **Reminders keep-or-kill now has its number** — US-17 / audit #8. `reminder_optin` has fired 0 times since instrumentation; the table has 2 rows ever. The opt-in is either unreachable or unwanted — decide surface-or-kill before any further engineering. Effort: product decision.
4. **Cloud-connect abandonment visibility** — US-09. First read: 5 started → 2 success. n is too small to judge, but the events carry no failure-step metadata; add a step/provider field to `cloud_connect_started`/abandon so the drop point is visible when n grows. Effort: S.
5. **Router unmatched 73% this week** — US-15 / audit #7. 68/93 unmatched vs ~48% baseline; the volume spike likely changed the query mix (new ZEUS/IL-6 blog post shipped this week). Cross-loop flag: chat-health owns retrieval fixes; this report is its pointer.

## New errors

Only the two YouTube-bot issues (1 event each, prod app only). Nothing new user-facing. The stale-A/B-test item (audit #5) was not re-checked this run — carried, see gaps.

## New feedback

None organic. The two `feedback_submissions` rows are the 2026-08-06/07 pipeline diagnostics from the RESEND_FROM_EMAIL fix (US-19) — pipeline provably works, zero real submissions through it yet.

## Data gaps

- `product_events` history starts 2026-08-06 → all W32 funnel numbers are a ~4-day partial week; W33 is the first clean week-over-week compare.
- `chat_opened` and `reminder_optin` report 0 for different reasons (dead emit site vs. likely-unreached UI) — distinguished by code inspection this run; the funnel table can't tell them apart on its own.
- Clarity is a 1–3-day sample by design (charter). A/B test status (audit #5) not pulled this run — add next run.
- No gaps from unreachable sources: all five Supabase tables, Sentry, Clarity ×2, and Gmail responded.

## Retro

- **Worked**: 3-worker fan-out (Supabase/Sentry/Clarity) + inline Gmail/git returned everything in one pass; the Supabase worker's "table starts 08-06" catch prevented a bogus +∞ week-over-week claim; verifying the two zero-events by reading emit sites turned a table row into the week's top finding.
- **Wasteful/missing**: nothing material; A/B status should be in the standard pull (added to next-run list, not the charter — trial first).
- **Charter amendments applied** (≤30 lines, changelog'd): (1) week-labeling convention — report week = the completed ISO data week; (2) zero-event rule — any funnel event reporting 0 must be classified dead-instrumentation vs. true-zero (inspect the emit site) before it enters the report.
- **Monthly pruning** (first run of August): least-earning line was LEARNINGS' Clarity-window entry — an exact duplicate of the charter's own Clarity bullet; deleted from LEARNINGS (charter keeps the operative copy).
- **Proposal for Brad (constitution, proposal-only)**: the constitution's repo rule "Verify `docs/products.md` is a symlink (git mode 120000) before committing" is STALE after the 2026-08-10 inversion — it now describes the exact state `scripts/check-symlinks.mjs` blocks. Suggested wording: "Verify `docs/products.md` is a REAL file (mode 100644), never a symlink."
- **Tier 3 grant**: not exercised this run (see backlog #1 rationale). 0 of 5 Brad-reviewed cycles used.
- Line counts: charter 90 (cap 200) · LEARNINGS 21 (cap 200) · this report ≤120.
