# Product-health — 2026-W36 (run 2026-09-05/06 NZ · window 2026-08-29T20:47Z → 2026-09-05T20:47Z)

Workflow integrity clean — both in-window `.github/workflows/` commits
(45a81d4, b4fbf49) are Brad's own (`--since="8 days ago"`, the W35 fix).

## TL;DR

- **CRITICAL: `docs/user-stories.md` silently lost US-12–US-28** — 17 stories
  (uploads, review, documents, chat, reminders US-17, email US-22/23, PDF,
  feedback, mobile, calendar, sentry-fix's US-27/28), Epics D–G, Coverage
  priorities. Commit `2285724` (09-01, "US-32 Phase 1 docs…") hit the file
  +18/−221 while its message describes only US-32 edits, and regenerated the
  HTML in the same commit — invisible for 4 days; code/tests still cite the
  deleted ids. Verbatim recovery:
  [notes/user-stories-lost-block.md](notes/user-stories-lost-block.md).
  Issue opened (Delivery).
- **The connector era started and all five new events are live**: `mcp_connect`
  3, `mcp_tool_call` 50, `mcp_import` 30 (US-35 shipped 09-04, defect A fixed
  09-05), `remote_change_applied` 65, `reminder_sent` 2 — the last matching
  the window's only two `last_sent` stamps exactly, closing W35 backlog #3.
  First-week MCP counts are Brad-led verification, not adoption (`client:
  other` on 45/50). ChatGPT app v1.0.0 still in Review (#60 staged for 1.0.1).
- **Reach fell a second week (results_viewed 261 → 214, -18%) but the scraper
  flood RECEDED**: drstanfield Clarity is organic again (4,133 sessions/3d vs
  200k) and `/pages/roadmap` is readable at 180 visits/3d — in line with
  pre-flood W32/W33 (216/178) while plan views are ~-39% vs those weeks. The
  dip now smells like conversion/behavior, not traffic collapse; the Shopify
  check (W35 #2) is the decider. Chat halved (331→152) on the YouTube bot's
  ebb (194→61); web chat held (~24); Discord appeared (8 events).

## What changed per source

- **Supabase — funnel**: results_viewed 214 (-18%). Uploads 5 → 3 saved with
  `upload_extract_failed` 0 — two silent drops at/around review: **W34's
  review-abandon watch triggers** (second occurrence; backlog #3).
  Corrections 6 (-45%). Cloud connects 7 → 3 (cumulative 26 → 14, ~54%).
  lab_rows_viewed 19 (+138% against the down week — plausibly Brad's own MCP
  verification; unattributable). Everything else moved with reach, as in W35.
- **Supabase — reminders/email**: 20 optins, **first optout ever** (emit
  production-verified; kill-ratio 1/109 vs the 30% threshold). All-time rows
  109 = 101 typed + 7 drive + **1 dropbox (first)**. 19 sent / 11 clicked
  (58% event ratio, uncohorted caveat) / 0 bounced / 0 complained.
  reminder_sent fired twice (typed; 08-31, 09-01), matching the stamps.
- **Supabase — MCP (new)**: 3 connects (2 dropbox, 1 drive), 50 tool calls
  (35 import_documents, 14 read_record, 1 get_plan; 8 refused), 30 mcp_import
  (27 folder, 2 drive_refused, 1 chatgpt_file). `client` resolves to `other`
  on 90% — Inspector/Claude Code sit outside the chatgpt/claude enum, so
  per-assistant adoption will be unreadable once real users arrive (#5 rider).
- **Supabase — chat**: 152 messages (93 user-authored), -54% on the YouTube
  bot ebb; match events 93 (61 youtube / 24 web / **8 discord — first**);
  blended true misses 17/42 = 40%; router_error 0. Chat-health's lane.
- **Sentry (14d)**: 2 new in-window — REMIX-69 "Ka`prod" (1 event, same
  minified class as W35's REMIX-63) and REMIX-6A `n.data.split` (1 event);
  watch only. PGRST303 class quiet since the 08-28 fix. userCount=0 on all 20
  (fifth week). **No errors from the live MCP/import paths** — the org has a
  single Sentry project, so both Fly apps report there: the clean signal is
  real, not a blind spot.
- **Clarity (3d)**: flood receded (TL;DR); microvitamin 2,533 sessions (-23%
  vs last pull), script errors 4.54% (flat), dead clicks 7.22%. No
  `/blogs/guides/*` page breaks either top-10 — guide traffic below the
  export's cutoff (gap).
- **Gmail/GitHub**: no feedback emails in 8d; 0 `feedback_submissions`; only
  `from-connector` issue ever is Brad's own path test (#53). OpenAI v1.0.0
  (C-Ggl3RkPf6el6): still in Review — only the 09-02 receipt email exists.
  Ops residue: deploy-notify issue #44 (09-01) never auto-closed.

## Funnel (7d vs prior 7d — rows appended to metrics.csv)

| step | W36 | W35 | note |
|---|---|---|---|
| results_viewed | 214 | 261 | -18%, second decline; upstream readable, ~flat |
| upload_started → saved | 5 → 3 | 6 → 5 | 2 drops, 0 extract fails — abandon watch triggers |
| cloud_connect started → success | 7 → 3 | 5 → 3 | cumulative 26 → 14 (~54%) |
| chat_opened | 25 | 30 | moved with reach |
| correction_made | 6 | 11 | small n |
| lab_rows_viewed / lab_row_added | 19 / 2 | 8 / 2 | viewed spike likely verify sessions |
| reminder_optin / optout | 20 / 1 | 31 / 0 | first optout ever; kill-ratio 1/109 |
| report_email sent / clicked | 19 / 11 | 29 / 10 | 58% event ratio (uncohorted); 0 bounced |
| reminder_sent | 2 | — | NEW; matches last_sent stamps exactly |
| mcp_connect / tool_call / import | 3 / 50 / 30 | — | NEW; Brad-led verification week |
| remote_change_applied | 65 | — | NEW (US-34) |
| chat_messages (context) | 152 | 331 | -54%, YouTube-bot ebb |

Zero-classification (charter rule): `upload_extract_failed` (fired W35),
`router_error` (fired W34), `report_email_bounced`/`complained` (webhook
verified W33) — all true zeros. Every event new this week fired.

## New errors

REMIX-69 + REMIX-6A (1 event each, minified/opaque — watch). Nothing
user-facing; nothing from the MCP surface in its first live week.

## New feedback

None organic (Gmail 0 in 8d; 0 submissions; 0 organic connector issues).

## Proposed backlog (build sessions decide)

1. **Restore US-12–US-28** — the CRITICAL above. XS: append the recovery
   block after US-35, regen HTML, delete the note. Rider (S): make
   `build-user-stories-html.ts` fail on a US-id referenced but not defined —
   this deletion would then have failed CI at the next regen.
2. **Sentry user/surface tags** — audit #9; FIFTH carry, still top:
   userCount=0 on all 20 while a whole new server surface just went live.
   Rider: per-recipient email dimension (click:send still uncohorted).
3. **Upload review-abandon event** — US-12/13 (currently deleted!): the W34
   watch triggered — twice now a started→saved gap with zero extract failures
   (W34 1 drop, W36 2). One event at review-close-without-save ends the guessing.
4. **Reach-dip diagnosis, now cheaper** — US-07: upstream readable and ~flat
   (180/3d vs 216/178 pre-flood) while plan views are -39% vs those weeks.
   Brad's Shopify sessions check (W35 #2) now splits traffic vs conversion;
   also eyeball whether the 09-03 storage-notice/connect redesign moved
   behavior. XS, ops.
5. **Cross-loop flag to chat-health**: YouTube bot ebbed 194→61; Discord rows
   appeared (8) — their segmentation should name it. Build-session rider:
   widen the `mcp_tool_call` client enum (claude-code, inspector, …) before
   adoption arrives, else per-assistant reads stay "other".

## Data gaps

- Guide-page traffic below Clarity's top-10 cutoff — US-33's usage signal
  has no readable number yet.
- MCP counters cannot separate Brad from real users at this n (by design,
  US-32); adoption reads start after the ChatGPT verdict.
- Web-chat-only miss split not computed (blended 40%; n=24) — chat-health's.
- Sentry counts soft (standing); OpenAI portal reachable only via Gmail. No
  unreachable sources: Supabase, Sentry, Clarity ×2, Gmail, GitHub answered.

## Retro

- **Orchestrator**: Fable, no fallback. 3 Sonnet workers, first-pass (the
  Supabase worker self-applied the pagination learnings unprompted).
- **The charter-named catch came from the orient read, not a worker**: Orient
  step 1 (read the spec yourself) hit dangling US-references; the shallow
  session clone (history starts 09-02) hid the deletion locally —
  archaeology needed the GitHub API.
- **Adversarial run-review**: PENDING at first commit (committed early per
  the constitution's truncation rule); findings folded in below once the
  reviewer reports.
- **Monthly pruning pass** (first September run): chat_opened saga + the
  superseded W32 reminder entry compacted; flood entry updated to receded;
  reminder_sent closed out; two entries added (silent-spec-deletion class,
  MCP first-week reading). LEARNINGS 99 → 104 lines: additions outweighed
  compaction; far under the 200 cap.
- **Success signal**: W35 #3 (send observability) shipped 08-30, verified by
  this run; W35 #2's flood half resolved itself. Sentry tags: fifth
  unactioned carry — flagging fatigue. Week's catches: the US-12–28 spec
  loss (before any build session noticed) + the review-abandon repeat.
- **Tier 3 grant**: not exercised (0 of 1) — the CRITICAL is a docs restore
  outside loop write scope (proposal + payload attached instead); no
  in-scope widget/health-core defect had a failing-test story.
- Vitals (machine `wc -l -c`): `84 5052` LOOP.md · `104 7521` LEARNINGS.md ·
  `111 13506` metrics.csv.
