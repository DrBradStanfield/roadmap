# Product-health — 2026-W36 (run 2026-09-05/06 NZ · window 2026-08-29T20:47Z → 2026-09-05T20:47Z)

Workflow integrity clean — both in-window `.github/workflows/` commits (45a81d4, b4fbf49) are Brad's own (`--since="8 days ago"`, the W35 fix).

## TL;DR

- **CRITICAL: `docs/user-stories.md` silently lost US-12–US-28** — 17 stories
  (uploads, review, documents, chat, reminders US-17, email US-22/23, PDF,
  feedback, mobile, calendar, sentry-fix's US-27/28), Epics D–G, Coverage
  priorities. Commit `2285724` (09-01, "US-32 Phase 1 docs…") hit the file
  +18/−221 while its message describes only US-32 edits, and regenerated the
  HTML in the same commit — invisible for 4 days; code/tests still cite the
  deleted ids. Issue [#62](https://github.com/DrBradStanfield/roadmap/issues/62)
  opened → **RESTORED 2026-09-06 on Brad's call** (verbatim from `bedc71b0`,
  evidence brought to W36, build gate added — backlog #1 done same day).
- **The connector era started and all five new events are live**: `mcp_connect`
  3, `mcp_tool_call` 50, `mcp_import` 30 (US-35 shipped 09-04), `remote_change_applied`
  65, `reminder_sent` 2 — the last matching the window's only two `last_sent`
  stamps, closing W35 backlog #3. First-week MCP counts are Brad-led
  verification, not adoption (`client: other` on 45/50). ChatGPT app v1.0.0
  still in Review (#60 staged for 1.0.1).
- **Reach fell a second week (results_viewed 261 → 214, -18%) but the scraper
  flood RECEDED**: drstanfield Clarity is organic again (4,133 sessions/3d vs
  200k) and `/pages/roadmap` is readable at 180 visits/3d — in line with
  pre-flood W32/W33 (216/178) while plan views are ~-39% vs those weeks: the
  dip smells like conversion/behavior, not traffic collapse (W35 #2's Shopify
  check decides). Chat halved on the YouTube bot's ebb; Discord appeared.

## What changed per source

- **Supabase — funnel**: results_viewed 214 (-18%). Uploads 5 → 3 saved with
  `upload_extract_failed` 0 — two silent drops at/around review: **W34's
  review-abandon watch triggers** (backlog #3). Corrections 6. Cloud connects
  7 → 3 (cumulative 26 → 14, ~54%). lab_rows_viewed 19 (+138% against the
  down week — plausibly Brad's MCP verification; unattributable). Everything
  else moved with reach, as in W35.
- **Supabase — reminders/email**: 20 optins, **first optout ever** (emit
  production-verified; kill-ratio 1/109 vs the 30% threshold). All-time rows
  109 = 101 typed + 7 drive + **1 dropbox (first)**. 19 sent / 11 clicked
  (58% event ratio, uncohorted) / 0 bounced / 0 complained. reminder_sent
  fired twice (typed; 08-31, 09-01), matching the stamps.
- **Supabase — MCP (new)**: 3 connects (2 dropbox, 1 drive), 50 tool calls
  (35 import_documents, 14 read_record, 1 get_plan; 8 refused), 30 mcp_import
  (27 folder, 2 drive_refused, 1 chatgpt_file). `client` is `other` on 90% —
  Inspector/Claude Code sit outside the chatgpt/claude enum (#5 rider).
- **Supabase — chat**: 152 messages (93 user-authored), -54% on the YouTube bot
  ebb; match events 93 (61 youtube / 24 web / **8 discord — first**); true misses 17/42 = 40%; router_error 0.
- **Sentry (14d)**: 2 new in-window — REMIX-69 "Ka`prod" (1 event, W35's
  REMIX-63 class) and REMIX-6A `n.data.split` (1 event); watch only. PGRST303
  class quiet since the 08-28 fix. userCount=0 on all 20 (fifth week). No
  MCP/import-path errors in the org's single project — but no in-window event
  is provably FROM the edu app, so a dead edu DSN would look identical (gap).
- **Clarity (3d)**: flood receded (TL;DR); microvitamin 2,533 sessions (-23%),
  script errors 4.54% (flat), dead clicks 7.22%; no `/blogs/guides/*` in either top-10 (gap).
- **Gmail/GitHub**: no feedback emails in 8d; 0 `feedback_submissions`; only
  `from-connector` issue ever is Brad's own path test (#53). OpenAI v1.0.0:
  still in Review — only the 09-02 receipt email exists. Ops residue:
  deploy-notify issue #44 (09-01) never auto-closed.

## Funnel (7d vs prior 7d — rows appended to metrics.csv)

| step | W36 | W35 | note |
|---|---|---|---|
| results_viewed | 214 | 261 | -18%, second decline; upstream readable, ~flat |
| upload_started → saved | 5 → 3 | 6 → 5 | 2 drops, 0 extract fails — abandon watch triggers |
| cloud_connect started → success | 7 → 3 | 5 → 3 | cumulative 26 → 14 (~54%) |
| chat_opened | 25 | 30 | moved with reach |
| correction_made | 6 | 11 | small n |
| lab_rows_viewed / lab_row_added | 19 / 2 | 8 / 2 | viewed spike likely verify sessions |
| reminder_optin / optout | 20 / 1 | 31 / 0 | first optout ever; kill-ratio 1/109 |
| report_email sent / clicked | 19 / 11 | 29 / 10 | 58% event ratio (uncohorted); 0 bounced |
| reminder_sent | 2 | — | NEW; matches last_sent stamps exactly |
| mcp_connect / tool_call / import | 3 / 50 / 30 | — | NEW; Brad-led verification week |
| remote_change_applied | 65 | — | NEW (US-34) |
| chat_messages (context) | 152 | 331 | -54%, YouTube-bot ebb |

Zero-classification (charter rule): `upload_extract_failed` (fired W35),
`router_error` (W34), `report_email_bounced`/`complained` (webhook verified
W33) — true zeros. Every event new this week fired.

## New errors

REMIX-69 + REMIX-6A (1 event each, minified/opaque — watch). Nothing user-facing; nothing attributable to the MCP surface's first live week.

## New feedback

None organic (Gmail 0 in 8d; 0 submissions; 0 organic connector issues).

## Proposed backlog (build sessions decide)

1. ~~**Restore US-12–US-28**~~ **DONE 2026-09-06** (Brad, via #62): block
   appended, headings/evidence updated to W36, and the rider shipped —
   `build-user-stories-html.ts` now refuses a build that drops a published
   story (`ALLOW_STORY_REMOVAL=1` to override) or references an undefined one.
2. **Sentry user/surface tags** — audit #9; FIFTH carry, still top:
   userCount=0 on all 20 while a whole new server surface just went live.
   Rider: per-recipient email dimension (click:send still uncohorted).
3. **Upload review-abandon event** — US-12/13 (restored, see #1): the W34
   watch triggered — twice now a started→saved gap with zero extract failures.
   One event at review-close-without-save ends the guessing.
4. **Reach-dip diagnosis, now cheaper** — US-07: upstream readable and ~flat
   while plan views are -39% vs pre-flood weeks. Brad's Shopify check (W35
   #2) splits traffic vs conversion; also eyeball whether the 09-03
   storage-notice/connect redesign moved behavior. XS, ops.
5. **Cross-loop flag to chat-health**: YouTube bot ebbed 194→61; Discord rows
   appeared (8). Build-session rider: widen the `mcp_tool_call` client enum
   (claude-code, inspector, …) before adoption arrives.

## Data gaps

- Guide-page traffic below Clarity's top-10 cutoff — US-33's signal unread.
- MCP counters cannot separate Brad from real users at this n (by design,
  US-32); adoption reads start after the ChatGPT verdict.
- Sentry counts soft (standing); web-chat miss split uncomputed (n=24); whether `health-tool-edu`'s DSN is live is
  unproven — no event provably from that app in 14d (verify: one deliberate
  test event, or check Fly env). OpenAI portal reachable only via Gmail. No
  unreachable sources: Supabase, Sentry, Clarity ×2, Gmail, GitHub answered.

## Retro

- **Orchestrator**: Fable, no fallback. 3 Sonnet workers, first-pass (the
  Supabase worker self-applied the pagination learnings unprompted).
- **The charter-named catch came from the orient read, not a worker**: Orient
  step 1 (read the spec yourself) hit dangling US-references; the shallow
  clone (history starts 09-02) hid it — archaeology needed the GitHub API.
- **Adversarial run-review** (Fable-tier, refute brief; every Supabase number
  recomputed via live exact-counts, recovery block byte-verified, write-scope
  diff + HTML regen byte-identical, Sentry/Clarity/Gmail/GitHub re-derived).
  Fixed by name: **#1 (HIGH)** TL;DR said "Issue opened" before it was — #62
  now exists; **#2** over the 150 cap — trimmed; **#3** "clean MCP Sentry
  signal" overstated (one project proves where edu events would LAND, not
  that the edu DSN emits) — reworded, gap added; **#4** "LEARNINGS 99 →" was
  98; **#5** `report_email_bounced` series row dropped — restored. Rebutted: none.
- **Monthly pruning pass** (first September run): two entries compacted, two
  updated, two added. LEARNINGS 98 → 109 lines; far under the 200 cap.
- **Restoration review (Fable 5.1, 2026-09-06, post-restore)**: verbatim
  diff vs bedc71b0 clean hunk-by-hunk; no HIGH. Fixed: F1 US-23 "unbuilt"
  body, F2 US-21 stale phase-1 note, F3 US-19 overstated loop process, F4
  symlinked invocation silently no-oped (realpath guard + test), F5 bare-id
  html regex, F7 literal asterisks, F8 "(currently deleted!)" in backlog #3.
  Rebutted: F6 (a missing html skips the removal check — that IS the
  first-build case; the CI test pinning US-12–28 covers it).
- **Success signal**: W35 #3 (send observability) shipped 08-30, verified by
  this run; W35 #2's flood half resolved itself. Sentry tags: fifth
  unactioned carry. Week's catches: the US-12–28 spec loss (before any build
  session noticed) + the review-abandon repeat.
- **Tier 3 grant**: not exercised (0 of 1) — the CRITICAL was a docs restore
  outside loop write scope (Brad applied it same day); no in-scope defect.
- Vitals (machine `wc -l -c`): `84 5052` LOOP.md · `109 7869` LEARNINGS.md ·
  `112 13661` metrics.csv · this report at the 150 cap post-review.
