# Product-health — 2026-W33 (run 2026-08-16 NZ · window 2026-08-08T20:47Z → 2026-08-15T20:47Z)

Window notes: the fleet's Monday→Sunday move (08-12) makes this window overlap
W32's published one by a day — prior-7d figures below are recomputed for
2026-08-01→08-08, not lifted from W32. The prior window holds only ~2.8
instrumented days (events live 08-06), so per-day rates are given wherever the
raw delta would mislead.

## CRITICAL — workflow integrity (charter tripwire)

`2d5b372` (2026-08-15, author `Claude|noreply@anthropic.com`) modified
`.github/workflows/ci.yml` — the first non-Brad workflows commit since this
check began. Diff reviewed: two `tsc --noEmit` gate steps (widget-src +
health-core), no secret access, no deploy-path or trigger changes; the commit
message claims live Brad authorization in an interactive session and matches
the sentry-fix loop's typecheck-gate narrative (its LEARNINGS closed that
enabler the same day). Content assessed benign — but authorization is
unverifiable out-of-band, so per charter this leads the report and the
"🎯 Decision needed" issue asks Brad to ack or repudiate. The same commit also
touched `packages/health-core/src/suggestions.ts` (`Object.hasOwn` →
`hasOwnProperty` iOS-compat, self-described as mechanical, no
threshold/evidence change — verified by reading the diff); flagged here because
clinical files sit above every tier, and Brad's ack should cover both.

## TL;DR

- **The retention engine is live and its first week worked**: 18 enrolments
  (17 typed + cloud auto-enrol; `reminder_optin_v2` 2 → 20 rows all-time),
  25 plan-ready emails, 10 clicked (40%), 1 bounce, **0 opt-outs, 0
  complaints** — kill-ratio 0% against the 30% threshold. W32's "strongest
  kill-signal yet" is formally reversed: the zero measured reach, not demand.
  The ~10:1 typed-vs-cloud bet (US-23) held almost exactly (17:1 this week).
- **`chat_opened` is STILL 0 after the 08-10 fix** — root cause found this
  run: `vite.config.site-chat.ts` never defines `VITE_SHOPIFY_SURFACE`, so
  `trackProductEvent` no-ops in the site-wide FAB bundle that carries most
  chat traffic. Tier 3 PR ships the define + a config test (see backlog #1).
  Chat itself grew +150% (260 messages, clean server-table comparison);
  router unmatched 70.4% — still chat-health's lane.
- Quiet error week: 1 new Sentry issue (platform-bot, 1 event); both W32 chat
  failures are resolved with zero post-fix events — and their W32 "86/14d and
  24/14d" figures turn out to have been lifetime counts (corrected in retro).

## What changed per source

- **Supabase — funnel**: first full instrumented week. Per-day rates flat on
  the core loop (results_viewed ~50/day), corrections steady (11), uploads
  down (2 vs 7 — small n), cloud connects 3 started → 1 success (cumulative
  8→3 over two weeks, still no failure-step metadata). lab_rows_viewed fell
  to 6 (from ~6.5/day novelty week to ~0.9/day). All-time product_events: 621.
- **Supabase — reminders/email (US-17/22/23, all shipped + deployed 08-13/14)**:
  18 `reminder_optin` events; rows now 17 typed + 3 google-drive; 25
  `report_email_sent`, 10 clicked, 1 bounced (webhook un-enrolled it —
  the machine works end-to-end in production), 0 `reminder_optout`, 0
  complaints.
- **Supabase — A/B (audit #5, carried gap from W32, now pulled)**: the
  Subheading test is still `active` after 4.2 months. All-time: A 245/5290 =
  4.63% vs B 219/5691 = 3.85% conversion (~+20% relative, ~2σ). It has done
  its job — propose: ship A, close the test (decision issue).
- **Sentry (14d)**: 1 new issue in 7d (`logTickError`, YouTube-bot, 1 event).
  Theme-owned `@shopify/events`/parseModule cluster continues (42 events/14d,
  still unignored). JAVASCRIPT-REMIX-1M and -3X both resolved, 0 events since
  their fixes (3X shows one ambiguous same-day event on fix day). userCount=0
  on all 22 issues — user/surface tags (audit #9) still missing.
- **Clarity (3d sample)**: drstanfield 4,490 sessions (+8.7%); /pages/roadmap
  still #3 at 178 visits (−18%); dead-clicks 9.2% (−0.8pp). microvitamin
  2,152 sessions (+16.8%); script errors 4.79% (consistent with the theme
  cluster).
- **Gmail**: no feedback emails in 8d. **Workflow integrity**: see CRITICAL.

## Funnel (7d vs prior 7d — rows appended to metrics.csv)

| step | W33 | prior | note |
|---|---|---|---|
| results_viewed | 351 | 149 | per-day flat ~50/day; raw delta = window artifact |
| upload_started → saved | 2 → 2 | 7 → 7 | 100% completion holds; volume down, small n |
| cloud_connect started → success | 3 → 1 | 5 → 2 | 8→3 cumulative; no failure-step metadata yet |
| chat_opened | 0 | 0 | **still dead on site-chat bundle** (backlog #1, PR open) |
| correction_made | 11 | 6 | steady real usage (US-04) |
| lab_rows_viewed / lab_row_added | 6 / 0 | 15 / 0 | viewed: real decline; added: UI live only ~1.5d (true zero so far) |
| reminder_optin / optout | 18 / 0 | 0 / 0 | first default-on week; kill-ratio 0% |
| report_email sent / clicked / bounced | 25 / 10 / 1 | — | US-22 first week; 40% CTR |
| chat_messages (context) | 260 | 104 | +150%, window-clean |

Zero-classification (charter rule): `chat_opened` dead-instrumentation on the
traffic-carrying surface (emit gated off at build; verified in
vite.config.site-chat.ts); `lab_row_added`, `upload_extract_failed`,
`reminder_optout` true zeros (emit sites read and live; tiny exposure windows
or genuinely no occurrences).

## New errors

Only `logTickError` (JAVASCRIPT-REMIX-61, 08-09, platform-bot, 1 event).
Nothing new user-facing.

## New feedback

None organic (Gmail 0 in 8d; feedback_submissions 0 in window).

## Proposed backlog (build sessions decide; #1 shipped by this run's Tier 3 grant)

1. **site-chat bundle never defines VITE_SHOPIFY_SURFACE** — US-15 AC2. The
   other half of W32 backlog #1: Brad's 08-10 ChatSection fix is correct but
   only reaches the widget bundle. Shipped as this run's Tier 3 PR (define +
   config test pinning surface flags for every bundle that mounts
   ChatSection). Effort: XS. The chatbot-embed surface (microvitamin, own UI)
   stays uninstrumented — still a product decision (W32 flag stands).
2. **Ship Subheading variant A and close the test** — audit #5. 4.2 months
   running, A +20% relative at ~2σ, and every extra week costs conversions on
   the B arm. Effort: XS + Brad's call (in the decision issue).
3. **Sentry user/surface tags** — audit #9, carried. userCount=0 on all 22
   issues; with chat and email volume climbing, impact stays unmeasurable.
   Effort: S.
4. **Cloud-connect failure-step metadata** — US-09, carried from W32 #4.
   Cumulative 8 started → 3 success and we still can't see the drop point.
   Effort: S.
5. **Watch lab_rows_viewed (US-21)** — 6 this week vs 15 in the novelty
   window, and the AC1 relayout landed 08-14 mid-decline. No action yet;
   W34 decides whether the relayout or the novelty wear-off owns the trend.

## Data gaps

- Prior-window funnel numbers cover only ~2.8 instrumented days (events live
  08-06); W34 vs W33 will be the first fully clean week-over-week.
- Schedule shift (Mon→Sun) makes this window overlap W32's published window
  by one day — one-off, self-corrects from next week.
- `product_events` carries no surface/store dimension: drstanfield and
  microvitamin traffic are indistinguishable (relevant to #1 and #3 above).
- `reminder_optin_v2` has no `status` column — tombstones live in the user's
  file, not the table (schema fact noted for future queries; not a defect).
- No unreachable sources this run: Supabase, Sentry, Clarity ×2, Gmail all
  answered.

## Retro

- **Orchestrator**: Fable (no tier fallback). 3 workers (Supabase/Sentry/
  Clarity, all Sonnet) + inline Gmail/git; all returned on first pass. The
  Supabase worker caught the missing `status` column and enumerated event
  names instead of assuming the list — both prevented wrong rows.
- **Corrected from W32**: the "86/14d listConversations + 24/14d sendMessage"
  figures were lifetime counts (Sentry `count` field is not statsPeriod-
  scoped); listConversations has been silent since 07-17. LEARNINGS updated;
  this run's counts sum the daily buckets.
- **Success signal — the loop is earning its run cost**: W32 backlog #1 was
  picked up by Brad the SAME DAY the report shipped (a632822, 08-10), #3
  (reminders keep-or-kill) became the week's biggest build (US-17/22/23).
  This run then caught that #1's fix didn't reach the right bundle — exactly
  the "funnel regression caught before Brad notices" the charter names.
- **Wasteful/missing**: nothing material. The A/B pull (added this run after
  W32's gap) cost one worker query and produced a decision-ready number.
- **Tier 3 grant**: exercised (1 of 1 allowed) on backlog #1 — see PR link in
  the delivery commit. Adversarial review findings and resolutions: see
  review addendum below.
- Vitals (`wc -l -c`): charter 80 / 4772B · LEARNINGS 46 / 3118B ·
  metrics.csv 41 lines.
