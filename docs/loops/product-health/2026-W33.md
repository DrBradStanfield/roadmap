# Product-health — 2026-W33 (run 2026-08-16 NZ · window 2026-08-08T20:47Z → 2026-08-15T20:47Z)

Window notes: the fleet's Monday→Sunday move (08-12) overlaps W32's published
window by a day — prior-7d figures are recomputed for 2026-08-01→08-08. The
prior window holds only ~2.7 instrumented days (events live 08-06T03:32Z), so
per-day rates are given wherever the raw delta would mislead.

## CRITICAL — workflow integrity (charter tripwire)

`2d5b372` (2026-08-15, author `Claude|noreply@anthropic.com`) modified
`.github/workflows/ci.yml` — a non-Brad workflows commit. The ci.yml diff is
exactly two `tsc --noEmit` gate steps (widget-src + health-core): no secret
access, no deploy-path or trigger changes. But the commit spans **24 files**,
including `packages/health-core/src/suggestions.ts` (`Object.hasOwn`→
`hasOwnProperty` iOS-compat; diff read — no threshold/evidence change),
`widget-src/src/storage/roadmap-store.ts` (sync-pending-marker rework, ~84
lines), `standalone/connect.ts` lift-failure wiring, and `sync-control.tsx`.
The message claims live Brad authorization and matches the sentry-fix loop's
typecheck-gate narrative; content assessed benign — but authorization is
unverifiable out-of-band, so per charter this led the report and issue #25.
**Resolved: Brad acked the commit live in this run's session (2026-08-16).**

## TL;DR

- **The retention engine is live and its first week worked**: 18 enrolments
  (17 typed + cloud auto-enrol; `reminder_optin_v2` 2 → 20 rows all-time),
  25 plan-ready emails, 10 clicked (40%), 1 bounce, **0 opt-outs, 0
  complaints** — kill-ratio 0% against the 30% threshold. W32's "strongest
  kill-signal yet" is formally reversed: the zero measured reach, not demand.
  The ~10:1 typed-vs-cloud bet (US-23) held almost exactly (17:1 this week).
- **`chat_opened` is STILL 0 after the 08-10 fix** — root cause found this
  run: `vite.config.site-chat.ts` never defines `VITE_SHOPIFY_SURFACE`, so
  `trackProductEvent` no-ops in the site-wide FAB bundle that carries most
  chat traffic. Tier 3 PR #23 ships the define + a config test (backlog #1).
  Chat itself grew +150% (260 messages, clean server-table comparison).
- **Router health is better than the headline number**: 119/169 match events
  carry no handles, but 92 of those are deliberate `router_skipped` classifier
  bypasses — TRUE router misses are 27/77 = **35%, below the ~48% baseline**.
  W32's published 73.1% likely conflated the same two things (adversarial
  review catch); chat-health should re-frame before acting on either figure.

## What changed per source

- **Supabase — funnel**: first full instrumented week. results_viewed ~50/day
  (flat vs ~55/day prior); corrections 11 (2.2→1.6/day, ~-29% at small n);
  uploads down (2 vs 7); cloud connects 3 started → 1 success (cumulative 8→3,
  still no failure-step metadata). lab_rows_viewed fell to 6 — ~8.7/day in its
  novelty window (live 08-07) → ~0.9/day now. All-time product_events: 621.
- **Supabase — reminders/email (US-17/22/23, shipped + deployed 08-13/14)**:
  18 `reminder_optin` events; rows now 17 typed + 3 google-drive; 25
  `report_email_sent`, 10 clicked, 1 bounced (webhook un-enrolled it — the
  machine works end-to-end in production), 0 `reminder_optout`, 0 complaints.
- **Supabase — A/B (audit #5, W32 gap, now pulled)**: Subheading test still
  `active` after 4.3 months. A 245/5290 = 4.63% vs B 219/5691 = 3.85%
  (~+20% relative, z≈2.0; conversions visitor-unique). It has done its job —
  propose: ship A, close the test (decision issue).
- **Sentry (14d)**: 1 new issue in 7d (`logTickError`, YouTube-bot, 1 event).
  Theme-owned `@shopify/events`/parseModule cluster continues (worker pull:
  42 events/14d, still unignored). JAVASCRIPT-REMIX-1M and -3X both resolved,
  nothing after their fixes (3X: one ambiguous fix-day event). userCount=0 on
  all 22 issues — user/surface tags (audit #9) still missing. Per-issue event
  COUNTS proved non-reproducible across pulls this run — see retro.
- **Clarity (3d sample)**: drstanfield 4,490 sessions (+8.7%); /pages/roadmap
  still #3 at 178 visits (−18%); dead-clicks 9.2% (−0.8pp). microvitamin
  2,152 sessions (+16.8%); script errors 4.79% (consistent w/ theme cluster).
- **Gmail**: no feedback emails in 8d. **Workflow integrity**: see CRITICAL.

## Funnel (7d vs prior 7d — rows appended to metrics.csv)

| step | W33 | prior | note |
|---|---|---|---|
| results_viewed | 351 | 149 | per-day flat ~50/day; raw delta = window artifact |
| upload_started → saved | 2 → 2 | 7 → 7 | 100% completion holds; volume down, small n |
| cloud_connect started → success | 3 → 1 | 5 → 2 | 8→3 cumulative; no failure-step metadata yet |
| chat_opened | 0 | 0 | **still dead on site-chat bundle** (backlog #1, PR #23) |
| correction_made | 11 | 6 | real usage; per-day 2.2→1.6 at small n |
| lab_rows_viewed / lab_row_added | 6 / 0 | 15 / 0 | viewed: real decline; added: UI live ~1.5d (true zero so far) |
| reminder_optin / optout | 18 / 0 | 0 / 0 | first default-on week; kill-ratio 0% |
| report_email sent / clicked / bounced | 25 / 10 / 1 | — | US-22 first week; 40% CTR |
| chat_messages (context) | 260 | 104 | +150%, window-clean |

Zero-classification (charter rule): `chat_opened` dead-instrumentation on the
traffic-carrying surface (emit gated off at build, verified); `lab_row_added`,
`upload_extract_failed`, `reminder_optout` true zeros (emit sites read and
live; tiny exposure windows or genuinely no occurrences).

## New errors

Only `logTickError` (JAVASCRIPT-REMIX-61, 08-09, platform-bot, 1 event) — nothing user-facing.

## New feedback

None organic (Gmail 0 in 8d; `feedback_submissions` 0 in window).

## Proposed backlog (build sessions decide; #1 shipped by this run's Tier 3 grant)

1. **site-chat bundle never defines VITE_SHOPIFY_SURFACE** — US-15 AC2. The
   other half of W32 backlog #1. Shipped as Tier 3 **PR #23** (define +
   failing-first test that forces every bundle through surface triage, both
   directions). Independent reviewer approved `0b6d09d`; its residuals for a
   build session: unguarded `execSync` in chatbot/upload configs, stale US-15
   test-status line. chatbot-embed stays uninstrumented (W32 product decision).
2. **Ship Subheading variant A, close the test** — audit #5. 4.3 months, A
   +20% relative at z≈2.0; every extra week costs B-arm conversions. Effort:
   XS + Brad's call (decision issue).
3. **Sentry user/surface tags** — audit #9, carried. userCount=0 on all 22
   issues; with chat and email volume climbing, impact stays unmeasurable.
4. **Cloud-connect failure-step metadata** — US-09, carried from W32 #4.
   Cumulative 8 started → 3 success; the drop point is still invisible.
5. **Watch lab_rows_viewed (US-21)** — real per-day decline, and the AC1
   relayout landed 08-14 mid-decline. W34 decides relayout vs novelty.

## Data gaps

- Prior-window funnel numbers cover ~2.7 instrumented days; W34 vs W33 is the
  first fully clean week-over-week. Window overlaps W32's by one day (schedule
  shift, one-off).
- `product_events` has no surface/store dimension: drstanfield vs microvitamin
  traffic indistinguishable (relevant to backlog #1/#3).
- Sentry per-issue counts are non-reproducible across pulls (retro) — this
  week's Sentry table treats counts as soft, first/lastSeen as reliable.
- `reminder_optin_v2` has no `status` column (tombstones live in the user's
  file — schema fact, not a defect).
- No unreachable sources: Supabase, Sentry, Clarity ×2, Gmail all answered.

## Retro

- **Orchestrator**: Fable (no fallback). 3 Sonnet workers + inline Gmail/git,
  all first-pass; the Supabase worker's schema checks prevented wrong rows.
- **Adversarial run-review (constitution)** — all headline numbers recomputed
  and held (incl. failing-first re-verified in a scratch worktree). Fixed by
  name: **#3** router-unmatched framing (92/119 were classifier skips; true
  miss 35% — report/metrics/user-stories re-framed); **#7** 2d5b372 footprint
  was understated (24 files, not 2 — CRITICAL + issue now carry it); **#9**
  my Sentry "W32's 86/24 were lifetime counts" explanation was itself WRONG:
  live pulls return count=1 for both issues, so the 86/24 origin is
  unidentified and per-issue counts are unstable — LEARNINGS rewritten as a
  warning, not an explanation; **#12** lab_rows "6.5/day" mis-derived (true
  ~8.7/day; conclusion stands); **#6** US-23 edit strayed onto a Tests line —
  moved to its Evidence line; **#14** decision issue + this addendum were
  pending at review time — both now closed. Rebutted: none.
- **Success signal**: W32 backlog #1 was picked up by Brad the SAME DAY the
  report shipped (a632822); #3 became the week's biggest build (US-17/22/23).
  This run then caught that #1's fix missed the traffic-carrying bundle —
  exactly the "funnel regression caught before Brad notices" the charter names.
- **Tier 3 grant**: exercised (1 of 1) — PR #23 MERGED via auto-ship (approve
  `0b6d09d`, CI green); deploy 31911537298 dispatched. Author live-verify
  BLOCKED — this container's egress denies drstanfield.com (named gap); the
  pipeline's own live-verify step + W34's chat_opened count are the checks.
- Vitals (`wc -l -c`): charter 80 / 4772B · LEARNINGS 50 / 3469B ·
  metrics.csv 41 lines.
