node_modules/
.env
.env.local
.env.*.local
dist/
dist-pages/
_site/
build/
.cache/
.react-router/
*.log
.DS_Store
.shopify/
.claude/*
!.claude/agents/
.mcp.json
fly.toml
*.js.map
docs/wiki/research/
docs/supplement-wiki.md
docs/knowledge-map/

# Local test artifacts / scratch — never commit (purged from history 2026-06-12)
tmp/

# One-off / scratch scripts — never commit (e.g. _purge-run.mjs, _purge-backup.mjs)
_*.mjs
_*.ts
_*.js

# Backups & data dumps — never commit (these can hold real user health data / PII)
*.bundle
*backup*
roadmap-supabase-backup*
*.dump
*.sql.gz
*.pgdump
exports/
dumps/

# Local cloud/local-first data stores — never commit (real user data lives in the
# user's own cloud or localStorage, never in this repo)
health-roadmap.json
health_roadmap_file*

# Sourcemaps — stripped before Shopify deploy; never tracked
*.map

# Code-review screenshot artifacts — local only, never tracked
.review-shots/
